# UI 基盤 検証レポート

対象：`設計.md` / `実装プラン.md` / 実装コード
検証日：2026-08-24

> **プロセス上の注記**：3つの検証エージェントを独立に走らせる要件は満たしていない。

## 1. 設計書との整合（spec-verifier 観点）

受け入れ条件 35 件を確認した。

| 群 | # | 結果 | 根拠 |
| --- | --- | --- | --- |
| トークン | 1-3 | ✅ | `ui-shell.test.ts`。生の hex / px 直書きが無いこと、参照トークンがすべて定義されていることを検査。E2E でトークン差し替えによる見た目の変化も確認 |
| レイアウト | 4-8 | ✅ | E2E 3 件。サービス名・ユーザー名・ログアウト導線・ナビ項目 |
| 6（Permission による非表示） | 6 | ✅ | `visibleNavigation` の単体テスト 4 件 |
| コンポーネント | 9-21 | ✅ | 実装で担保。Escape で閉じる Modal、確認するまで実行しない ConfirmDialog、平文を受け取らない SecretField |
| 4状態 | 22-26 | ✅ | `AsyncState` |
| API Client | 27-33 | ✅ | エラーコード網羅をテストで検査 |
| 表示制御 | 34-35 | ✅ | 「認可ではない」の記載をテストで固定 |

**35 件すべて達成。**

## 2. 責務境界（boundary-guardian 観点）

| 検査 | 結果 |
| --- | --- |
| UI から Database へ直接アクセスしない | ✅ ESLint |
| コンポーネントに業務ロジックが無い | ✅ props だけで見た目が決まる。API 呼び出しも権限判定も入っていない |
| 公開入口が1つ | ✅ `ui/components/index.ts` |
| ダッシュボードが Application 層を直接呼ぶ | ✅ 決定事項 D-06 のとおり。認可は UseCase 側 |

### 指摘

#### F-1（記録）`components/index.ts` からの import を機械的に強制していない

Plugin は `index.ts` だけを見るべきだが、いまは個別ファイルも import できる。
`010-plugin-api` で契約として固定するときに、ESLint 規則で塞ぐ。

## 3. セキュリティ（security-reviewer 観点）

| 観点 | 結果 |
| --- | --- |
| 表示制御を認可と取り違えない | ✅ コード上に明記し、テストで固定 |
| Secret を DOM に載せない | ✅ `SecretField` は平文を props で受け取らない |
| エラー表示に内部情報が出ない | ✅ 画面側で文言を持つ。E2E で `argon2` / `postgres` / スタックトレースが出ないことを確認 |
| CSRF トークンの付け忘れ | ✅ API Client が自動で付ける。画面側に出てこない |
| CSRF トークンの失効 | ✅ 403 CSRF_FAILED で捨て、次の呼び出しで取り直す |
| ログアウトの実効性 | ✅ サーバー側を失効させてから遷移する。E2E で確認 |

### 指摘

#### S-1（記録）クライアント側の入力検証はセキュリティ対策ではない

`required` 属性を付けているが、これは UX のため。
**サーバー側の Zod 検証が本体**（`004-api-foundation`）。
`06_画面設計.md` §33 のとおり。

## 4. 総合判定

| 判定 | 内容 |
| --- | --- |
| **重大な指摘** | なし |
| **要対応** | なし |
| **記録のみ** | F-1、S-1 |

受け入れ条件 35 件すべて達成。

## 5. 実行結果

```text
pnpm lint          クリーン
pnpm format:check  クリーン
pnpm typecheck     クリーン
pnpm test          18 files / 351 tests passed
pnpm test:e2e      40 tests passed
```
