# 046 NUL 文字と範囲外の日時・日数で 500 になるものを直す 実装プラン

> **（2026-09-25）ユーザー裁定 1〜4 を反映した版。** 裁定 2（対になっていないサロゲートも断る）で L1・L2 の規則と #2〜#4・#7・#9・#13〜#22・#24〜#26 などの期待が変わり、
> 裁定 3・4 で G11・G12（#47〜#51）が通常の作業になった。**裁定待ちの項目は無い。**

## 1. 前提

### 参照した文書

| 文書 | 用途 |
| --- | --- |
| [`設計.md`](設計.md)（`48fadb1`） | **唯一の入力仕様。** §10 の受け入れ条件 **#1〜#54 を全件**テストへ対応づける（§4）。条件付きの条件は無い |
| `docs/設計/045-campaign-input-500/設計.md`・`実装プラン.md` | `ValidationError` の `details` の組み立て、UseCase の検査の置き方、テストの手本、件数の突き合わせの形 |
| `docs/設計/043-api-input-fixes-rest/設計.md` §9 | Data API の例外の先例（`PluginDataInputError` を使う範囲） |
| `docs/設計/010-plugin-api/設計.md` §9 | 互換性の判断の追記の形（`043` の追記） |
| `docs/設計/037-sns-x/設計.md` §9.4・`040-sns-threads/設計.md` §9.5・`042-social-api-input-fixes/設計.md` §9.2 の 7 | 配信 Plugin の片割れの検査（**変えない**。設計 §6.8）と、正規表現で検出する先例 |
| `docs/設計/039-social-credential-fields/実装プラン.md` §8 の 35 | E2E をコンテナで流す手順（**本件では流さない**。§3 の T26） |
| `CLAUDE.md` | 依存の向き、テスト先行、1 コミット = 1 論理変更、公開リポジトリであること |

### 確定事項（設計書から。プランで蒸し返さない）

* **文字の規則**（設計 §6.2）：NUL（U+0000）と**対になっていないサロゲート**を含む文字列（値・キー・入れ子）は断る（ユーザー裁定 1・2）。
  422 の `details` のキーは**最上位の項目名**（本文の最上位のキーに含むとき・本文がオブジェクトでないときは `_`）。値は NUL なら `使用できない文字（NUL）が含まれています。`、
  片割れなら `使用できない文字（対になっていないサロゲート）が含まれています。`（同じ項目に両方あれば NUL が先の 2 つ）。送った値を載せない。Zod より先
* **検出**：片割れは正規表現 `/[\uD800-\uDBFF](?![\uDC00-\uDFFF])|(?<![\uD800-\uDBFF])[\uDC00-\uDFFF]/`（`u` フラグなし）。`isWellFormed` / `toWellFormed` は `lib`（`ES2023`）に型が無いので使わず、`lib` も変えない（設計 §6.2）
* **置き場は 4 つ**（設計 §4）：L1 `api/route.ts`（認証・Permission の後、Zod の前。`body` を宣言し `bodyKind` が `json` の本文と、`query` を宣言したルートのクエリ）、
  L2 UseCase（設計 §4.2 の **45 組**。`analytics.record` の `key` だけは既存の検査の後で片割れだけを見る）、L3 `plugin/store.ts`、L4 `publish.ts`・`run-job.ts`（**断らずに U+FFFD に置き換えて記録**）。
  パスの `{id}`・ヘッダ・`/auth/callback` のクエリ・`bodyKind: 'raw'` は見ない
* **L1 は本文を置き換えない**（断るだけ。本文はそのまま Zod へ）。L2 も値を返さない（`assertUsableText` は検査だけ）
* **L2 は `defineUseCase` に一律に置かない**。ID の項目・既に形の規則で両方を断る項目（`loginId`・`status`・`metric`・作成の `provider` など）は対象外（設計 §4.2・§4.3）
* 認証の要らない口（`login`・`password-reset` の 2 つ・`setup`・`collect`・`authorize`）も **422**（設計 §6.3）
* Data API は `data-api.ts` を変えない（UseCase の `ValidationError` がそのまま reject になる）。`analytics.record` の `siteId` は UseCase が `isUuidShape` で見る（設計 §9.2）
* **N1〜N3**（ユーザー裁定 3）：`status` の列挙（`isSiteStatus` / `isCampaignStatus`。文言 `状態の値が正しくありません。`）、`analytics.record` の `siteId` の存在（書き込みと同じトランザクション。`存在しないWebサイトです。`）、
  `value` の上限（`Math.floor(value) > Number.MAX_SAFE_INTEGER` で `9007199254740991以下の数値を指定してください。`）（設計 §9.2）
* **N4**（ユーザー裁定 4）：`dateOnly`・`toDateOnly` の年を 4 桁に（設計 §6.4）
* Store は NUL と片割れを `PluginStoreError`（`JSON として保存できない文字（NUL・対になっていないサロゲート）を含む値`）、`keys(NUL か片割れを含む接頭辞)` は `[]`。`setSecret` は変えない（設計 §9.3）
* L4：`toStorableText` は秘匿の**後**、長さの切り詰めの**前**。`externalUrl` と `manual()` の URL の NUL は `isValidExternalHttpsUrl` が偽にして捨てる（設計 §9.4）
* B2：`isCalendarDate` が年 `0000` を偽に。422 のキーは既存の `to` / `metricDate`（設計 §6.4）
* B3：`SCHEDULED_AT_MIN_MS = -62135596800000`・`SCHEDULED_AT_MAX_MS = 253402300799999`、`isValidScheduledAt`、UseCase で `ValidationError('SocialPost', 'scheduledAt', '0001-01-01T00:00:00Z から 9999-12-31T23:59:59.999Z までの日時を指定してください。')`。Zod は `.describe()` だけ（設計 §6.5）
* B4：`ACCESS_LOG_PRUNE_MAX_DAYS = 36500`、Zod `.max(36500, '36500以下で指定してください。')`、`pruneAccessLogs` も同じ上限（設計 §6.6）
* **公開 Plugin API は TSDoc だけ**（`store.ts`・`social.ts`・`data.ts`）。`PLUGIN_API_VERSION` は変えない。**`plugins/` は変えない**（配信 Plugin の片割れの検査は残す。設計 §6.8）
* **画面は変えない。E2E は足さず、流さない**（設計 §7・§10.14）
* **既存テストの変更**：`api/campaign-link-input.integration.test.ts` の `MALFORMED` の `['NUL 文字', () => '\u0000']` の行を外す（設計 §10.13）。
  **加えて、片割れを 2xx で保存できることを前提にした Core のテストが見つかったら、その期待を 422 / `ValidationError`（サロゲートの文言）に変えてよい**（設計 §10.13。検索では見つかっていない。変えたら §8 に記録する）。それ以外の既存テストは変えない
* マイグレーションは無し

### 既存コードの確認結果

| 確認したもの | 事実 |
| --- | --- |
| `apps/web/src/api/route.ts` | 順序は Rate Limit → Bearer の判定 → （非安全なメソッド）本文を JSON として読む（`rawBody`）→ CSRF → `sessionOnly` → 認証 → `requirePermission` → 本文の `validate` → クエリの `validate`（`searchParams` を `Record<string, string>` に集める）→ ハンドラ。L1 は `requirePermission` の直後に置く |
| `apps/web/src/api/validation.ts` | Zod のルートの誤りのキーは `_`。`ValidationDetails = Record<string, readonly string[]>` |
| `apps/web/src/domain/repository.ts` | `ValidationError(resource, field, detail, details?)`。`message` は `<resource> の <field> が不正`。`route.ts` は `details ?? { [field]: [detail] }` を 422 にする |
| `apps/web/src/application/authorization/use-case.ts` | `defineUseCase` は Permission を見てからハンドラを呼ぶ。**変えない** |
| `apps/web/src/plugin/store.ts` | `serialize()` が `JSON.stringify` と大きさを見て `PluginStoreError` を投げる。`keys(prefix)` は `like` で問い合わせる |
| `apps/web/src/application/social/publish.ts` | `safeText`（`redactSecrets` → `redactCredentialValues`）を `redactAttemptResult` が `reason`・例外の文言に掛け、`normalizedVerdict` が長さをそろえる。`ok: true` の `externalId` は秘匿を通らない。記録の失敗は `social post publish aborted` で `unrecorded` |
| `apps/web/src/domain/social/publishing.ts` | `ok: true` の `externalUrl` は `isValidExternalUrl` が偽なら `null` |
| `apps/web/src/application/jobs/run-job.ts` | `jobErrorText = truncateError(redactSecrets(message))` |
| `apps/web/src/domain/social/social.ts` | `isValidExternalHttpsUrl`（非公開）を `isValidMediaUrl`・`isValidLink`・`isValidExternalUrl` が使い、`isValidManualUrl` の絶対 URL は `isValidExternalUrl` |
| `apps/web/src/domain/analytics/analytics.ts` | `isCalendarDate`（非公開）→ `isValidRange`。`assertRange`（UseCase）・`assertRollupRange`（ルート）・`recordAnalytics` が使う。`isValidBreakdownKey` は制御文字（NUL を含む）と長さを見るが片割れは通す |
| `apps/web/src/application/analytics/analytics-use-cases.ts` | `rangeOf`（`list`・`listPage`）と `breakdownPage`（`breakdown`・`topPathsPage`）が入力を検査する。`recordAnalytics` は `metricDate` → `metric` → `source` → `key` → `value` の順に検査し `siteId` を見ない |
| `apps/web/src/application/social/social-use-cases.ts` | `createSocialPost` は a（本文）→ b（アカウント）→ … → 配信 Plugin の `validate()`（`checkPublisherLimits`）。`updateSocialPost` も本文の検査が先頭 |
| `plugins/sns-x-api/x-text.ts`・`sns-x-manual/x-text.ts`・`sns-threads/threads-text.ts`・`sns-bluesky/social.ts` | `validate()` が本文・`link` の片割れを `body` の問題（`本文に扱えない文字が含まれています。`）で返す。**変えない**（設計 §6.8） |
| `apps/web/src/domain/site/site.ts`・`domain/campaign/campaign.ts` | `isSiteStatus`・`isCampaignStatus`（N1 で使う） |
| `apps/web/src/domain/analytics/day.ts`・`infrastructure/campaign-repository.ts` | `dateOnly`（公開）と `toDateOnly`（非公開）が同じ実装で、年を `getFullYear()` のまま文字列にする（N4） |
| `apps/web/src/app/api/v1/analytics/rollup/route.ts` | `pruneOlderThanDays: z.coerce.number().int().min(1).optional()`。削除は集計の後、`system.manage` を見てから |
| `tsconfig`（`apps/web`・base） | `lib` は `ES2023`。**`String.prototype.isWellFormed` / `toWellFormed`（ES2024）は型に無い**。`lib` を変えず、正規表現で見る（§7 の 4） |
| `apps/web/src/api/campaign-link-input.integration.test.ts` | `MALFORMED` は #13 の `it.each` で `siteIds` / `socialPostIds` の `POST` に使う。NUL の行は `UUID の形で指定してください。` を期待している |
| 片割れを使う既存テスト | `plugin/bluesky-manual-url.test.ts`・`sns-threads-text.test.ts`・`sns-threads.test.ts`・`sns-threads-retry.test.ts`・`sns-x-api.test.ts`・`sns-x-manual.test.ts`・`sns-x-text.test.ts`（Plugin の関数を直接呼ぶ単体テスト。Core を通らないので変わらない）、`domain/jobs/job.test.ts`（`truncateError`）。**Core を通して片割れを 2xx で保存するテストは無い**（2026-09-25 に検索） |
| 手本 | Route Handler を直接叩く：`api/non-uuid-id.integration.test.ts`（セッション・CSRF・未認証の 401 の作り方）・`api/list-query.integration.test.ts`（Bearer）。Data API：`plugin/data-api-campaign-links.integration.test.ts`。Store：`plugin/store.integration.test.ts`。配信：`application/social/publish.integration.test.ts`（偽の publisher・`publishDuePosts`）。ジョブ：`application/jobs/run-job.integration.test.ts`。Plugin の設定：`plugin/example-plugin.integration.test.ts`（`activate()`）。OpenAPI：`api/campaign-openapi.test.ts`。文書の静的検査：`plugin/campaign-link-docs-static-checks.test.ts`。ログの捕まえ方：`api/social-post-create.integration.test.ts` の `capture()` |

## 2. 変更対象ファイル

| ファイル | 新規/変更 | 内容 | 条件 |
| --- | --- | --- | --- |
| `apps/web/src/domain/text.ts` | 新規 | `NUL_MESSAGE`・`LONE_SURROGATE_MESSAGE`・`containsNul`・`containsLoneSurrogate`・`unusableTextDetailsOf`・`toStorableText` | #1〜#5 |
| `apps/web/src/domain/social/social.ts` | 変更 | `isValidExternalHttpsUrl` が NUL を含む値を偽に。`SCHEDULED_AT_MIN_MS`・`SCHEDULED_AT_MAX_MS`・`isValidScheduledAt` | #6・#40 |
| `apps/web/src/api/route.ts` | 変更 | L1（NUL・片割れの検査） | #7〜#21 |
| `apps/web/src/application/text-input.ts` | 新規 | `assertUsableText(resource, fields)` | #22〜#26 |
| `apps/web/src/application/site/site-use-cases.ts` | 変更 | L2（`site.list`・`create`・`update`）。N1 の `status` | #22・#26・#47 |
| `apps/web/src/application/campaign/campaign-use-cases.ts` | 変更 | L2（`campaign.list`・`create`・`update`）。N1 の `status` | #22・#26・#47 |
| `apps/web/src/application/social/social-use-cases.ts` | 変更 | L2（`social.account.list`・`create`・`update`、`social.post.create`・`update`）。B3 の `scheduledAt` | #21・#22・#24・#26・#41・#42 |
| `apps/web/src/application/user/user-use-cases.ts` | 変更 | L2（`user.list`・`create`・`update`） | #22 |
| `apps/web/src/application/webhook/webhook-use-cases.ts` | 変更 | L2（`webhook.create`） | #22 |
| `apps/web/src/application/api-token/api-token-use-cases.ts` | 変更 | L2（`apiToken.create`） | #22 |
| `apps/web/src/application/system-settings/system-settings-use-cases.ts` | 変更 | L2（`systemSettings.update`） | #22 |
| `apps/web/src/application/plugin/plugin-settings-use-cases.ts` | 変更 | L2（`plugin.settings.save` の `values`） | #22・#24 |
| `apps/web/src/application/analytics/analytics-use-cases.ts` | 変更 | L2（`rangeOf` の `source`・`key`、`breakdownPage` の `source`、`recordAnalytics` の `key`（既存の検査の後））。`recordAnalytics` の `siteId` の形。N2・N3 | #22・#26・#27・#48・#49 |
| `apps/web/src/plugin/store.ts` | 変更 | L3 | #30・#31 |
| `apps/web/src/application/social/publish.ts` | 変更 | L4（`safeText`・`externalId`） | #32〜#34 |
| `apps/web/src/application/jobs/run-job.ts` | 変更 | L4（`jobErrorText`） | #36 |
| `apps/web/src/domain/analytics/analytics.ts` | 変更 | `isCalendarDate` の年の下限、`ACCESS_LOG_PRUNE_MAX_DAYS` | #37〜#39・#44〜#46 |
| `apps/web/src/application/analytics/rollup.ts` | 変更 | `pruneAccessLogs` の上限 | #45 |
| `apps/web/src/app/api/v1/analytics/rollup/route.ts` | 変更 | `pruneOlderThanDays` の `.max()`・`.describe()` | #44・#46 |
| `apps/web/src/api/schemas/social.ts` | 変更 | 作成・更新の `scheduledAt` の `.describe()` | #46 |
| `apps/web/src/domain/analytics/day.ts`・`apps/web/src/infrastructure/campaign-repository.ts` | 変更 | N4（年を 4 桁に） | #50・#51 |
| `packages/plugin-api/src/store.ts`・`social.ts`・`data.ts` | 変更 | TSDoc だけ（設計 §9.5） | #53・#54 |
| `docs/Plugin開発ガイド.md`・`docs/マニュアル/SNS投稿API仕様.md` | 変更 | 設計 §12 | #54 |
| `docs/設計/010-plugin-api/設計.md`・`045-campaign-input-500/設計.md`・`037-sns-x/設計.md`・`040-sns-threads/設計.md`・`042-social-api-input-fixes/設計.md` | 変更 | 設計 §12（§9 の追記・注記） | — |
| `apps/web/src/api/campaign-link-input.integration.test.ts` | 変更 | `MALFORMED` の NUL の行を外す | 設計 §10.13 |
| `apps/web/src/domain/text.test.ts` | 新規 | A | #1〜#5 |
| `apps/web/src/domain/social/external-url-nul.test.ts` | 新規 | A | #6 |
| `apps/web/src/api/nul-input.integration.test.ts` | 新規 | C | #7〜#12 |
| `apps/web/src/api/nul-public-input.integration.test.ts` | 新規 | C | #13〜#17 |
| `apps/web/src/api/lone-surrogate-input.integration.test.ts` | 新規 | C | #18〜#21 |
| `apps/web/src/application/text-input.integration.test.ts` | 新規 | B | #22〜#25 |
| `apps/web/src/plugin/data-api-text-input.integration.test.ts` | 新規 | B | #26〜#29 |
| `apps/web/src/plugin/store-text.integration.test.ts` | 新規 | B | #30・#31 |
| `apps/web/src/application/social/publish-text.integration.test.ts` | 新規 | B | #32〜#35 |
| `apps/web/src/application/jobs/run-job-text.integration.test.ts` | 新規 | B | #36 |
| `apps/web/src/domain/analytics/analytics-date-range.test.ts` | 新規 | A | #37 |
| `apps/web/src/api/analytics-date-range.integration.test.ts` | 新規 | C | #38・#44 |
| `apps/web/src/application/analytics/analytics-date-range.integration.test.ts` | 新規 | B | #39・#45 |
| `apps/web/src/domain/social/scheduled-at.test.ts` | 新規 | A | #40 |
| `apps/web/src/api/scheduled-at-range.integration.test.ts` | 新規 | C | #41・#43 |
| `apps/web/src/application/social/scheduled-at-range.integration.test.ts` | 新規 | B | #42 |
| `apps/web/src/api/input-range-openapi.test.ts` | 新規 | A | #46 |
| `apps/web/src/plugin/data-api-record-and-status.integration.test.ts` | 新規 | B | #47〜#49 |
| `apps/web/src/domain/analytics/date-only-year.test.ts` | 新規 | A | #50 |
| `apps/web/src/api/date-only-year.integration.test.ts` | 新規 | C | #51 |
| `apps/web/src/plugin/text-input-docs-static-checks.test.ts` | 新規 | E | #54 |

**変えないもの**：`apps/web/src/plugin/data-api.ts`、`application/authorization/use-case.ts`、`api/validation.ts`・`openapi.ts`・`registry.ts`、`app/api/v1/**/route.ts`（`rollup` を除く）、
`app/**` の画面、`ui/**`、`plugins/`、`packages/plugin-api` の `index.ts`・`version.ts`、`migrations/`、`docs/仕様書/`、既存のテスト（§1 の確定事項の 1 行と、許された期待の変更を除く）。

## 3. タスク分解

各グループは「テストを書く（赤）→ 実装する（緑）」の順。コミットは `test:` と `fix:` / `feat:` / `docs:` を分ける（テストだけのコミットが赤になるのは `045` と同じく許す。§8 に記録する）。

### G1. 文字の規則の純関数（設計 §4.1・§6.2・§10.1 の #1〜#5）

#### T1. テストを書く（赤）

* **何をするか**：`domain/text.test.ts`。#2 は片割れの場所（先頭・途中・末尾・逆順・絵文字の直後）を表で回す。#3 は戻り値のキーの順・文言の順（NUL が先）・同じ項目で文言が重ならないことを見る。
  #4 は置き換え前後の `length` の一致と、絵文字（対）が変わらないことを見る。**テストの中の文字列は `\ud800` などのエスケープで書き、U+FFFD と絵文字は `String.fromCodePoint(0xfffd)`・`String.fromCodePoint(0x1f44d)` で作る**（ソースに壊れた文字を置かない）
* **完了条件**：`domain/text.ts` が無いので全件が赤
* **依存**：無し

#### T2. `domain/text.ts` を足す（緑）

* **何をするか**：6 つを足す。`unusableTextDetailsOf` は最上位がプレーンなオブジェクトならキーごとに（キー自体に含むときは `_`）、配列・文字列・その他は `_`。入れ子はプレーンなオブジェクト（`Object.getPrototypeOf` が `Object.prototype` か `null`）と配列だけをたどる。
  片割れの判定は §1 の正規表現（`u` フラグなし）。`toStorableText` は NUL と片割れを U+FFFD に置き換える（片割れは同じ正規表現に `g` を付けたもの）
* **完了条件**：#1〜#5 が緑。`pnpm typecheck`・`pnpm lint`（Domain が他の層に依存しない）
* **依存**：T1

### G2. URL の判定（設計 §9.4・#6）

#### T3. テストを書く（赤）

* **何をするか**：`domain/social/external-url-nul.test.ts`。4 つの判定に `'https://x.example.com/p\u0000'` → 偽、NUL の無い同じ URL → 真。`isValidManualUrl` の `/` で始まる値（`isSafeReturnTo`）が NUL を既に断っていることも対照として見る
* **完了条件**：NUL の 4 件が赤
* **依存**：無し

#### T4. `isValidExternalHttpsUrl` に NUL の判定を足す（緑）

* **完了条件**：#6 と既存の `domain/social/*.test.ts`・`plugin/bluesky-manual-url.test.ts`（Plugin の URL の判定に `isValidManualUrl` を使う）が緑
* **依存**：T2（`containsNul`）・T3

### G3. HTTP の入口（L1。設計 §6.2・§6.3・§10.2〜§10.4）

#### T5. テストを書く（赤）

* **何をするか**：
  * `api/nul-input.integration.test.ts`（#7〜#12）。#7 は設計 §1.2.1 **表 H の 36 件**を 1 つの表（操作・ルートの関数・メソッド・パス・通る本文・値を足す場所・期待するキー）にし、**NUL と片割れの 2 通り**で `it.each` を回す（72 件）。
    認証はセッション（`non-uuid-id.integration.test.ts` の `issueSessionToken` とヘッダの組み方を写す）。作成は行数、更新は前後の `GET` で「変わっていない」を見る。
    `PUT /plugins/{id}/settings` は `example-plugin` を `activate()` してから（`example-plugin.integration.test.ts` を写す）。`POST /social/posts` の `externalRef` は Bearer の Token（`social.write`）で送る（冪等キーは Token のときだけ）
  * `api/nul-public-input.integration.test.ts`（#13〜#17）。`/setup` は利用者が 0 人の DB で始め、`users`・`user_roles`・`auth_audit_logs`・`sessions` を各ケースの前に空にする
  * `api/lone-surrogate-input.integration.test.ts`（#18〜#21）。本文は `JSON.stringify` がそのまま `\ud800` のエスケープを書くので、手で書かずに JS の文字列を渡す。
    #21 の偽の provider は、`validate()` が片割れを見つけたら `body` の問題を返し、呼ばれた回数を数える（`registerPublisher` で登録。`social-post-create.integration.test.ts` の `publisherFor` を写す）
  * **`api/campaign-link-input.integration.test.ts` の `MALFORMED` から `['NUL 文字', () => '\u0000']` の行を外す**（設計 §10.13。同じ入力は #9 が新しい期待で見る）
* **完了条件**：#7〜#11・#13〜#21 の NUL / 片割れの条件が赤（いまは 500・409・2xx・401・400、#21 は Plugin の文言で `validate()` が呼ばれる）。#10 の 401 / 403 / `CSRF_FAILED`、#12、#21 の絵文字は緑のまま
* **依存**：無し（期待する文言は設計から写す）

#### T6. `route.ts` に L1 を足す（緑）

* **何をするか**：`requirePermission` の直後に、`definition.body !== undefined` かつ `bodyKind` が `json` なら `unusableTextDetailsOf(rawBody ?? {})`、`definition.query !== undefined` なら集めたクエリの `Record` に `unusableTextDetailsOf` を掛け、
  まとめて（本文 → クエリの順。同じキーは文言を重ねない）1 つでもあれば `errorResponse('VALIDATION_ERROR', details, cors)`。本文・クエリの Zod の呼び方は変えない（置き換えはしない）。CSRF の `bodyToken` の読み方も変えない
* **完了条件**：#7〜#21 が緑。`api/*.test.ts`・`api/*.integration.test.ts`（`campaign-link-input` を含む）・`social*.integration.test.ts` が緑
* **依存**：T2・T5
* **コミット**：T5（テストと既存テストの 1 行）と T6 を分ける

### G4. UseCase（L2）と Data API（設計 §4.2・§9.2・§10.5・§10.6）

#### T7. テストを書く（赤）

* **何をするか**：
  * `application/text-input.integration.test.ts`（#22〜#25）。#22 は設計 §4.2 の **45 組**を表にし（UseCase・呼び方・項目・値を足した入力）、**NUL と片割れの 2 通り**で回す（`analytics.record` の `key` は片割れだけ。NUL は #25）。
    例外が `ValidationError` で `field`・`details`（それぞれの文言）、`message` に値が無いこと、`pg` の `DatabaseError` でないこと、作成は行数が増えないことを見る。`social.post.update` の `externalRef` は無い（作成だけ）
  * `plugin/data-api-text-input.integration.test.ts`（#26〜#29）。`data-api-campaign-links.integration.test.ts` の `apiFor` と利用者の作り方を写す
* **完了条件**：#22（NUL は `DatabaseError`、片割れは成功しているので赤）・#24・#26・#27 が赤。#23・#25・#28・#29 と #24 の絵文字は緑のまま
* **依存**：無し

#### T8. `application/text-input.ts` と 45 組を足す（緑の大部分）

* **何をするか**：`assertUsableText(resource, fields)`：`undefined`・`null` の項目を飛ばして `unusableTextDetailsOf` を掛け、1 つでもあれば `ValidationError(resource, 先頭のキー, 先頭の文言, details)`。
  各 UseCase のハンドラの**先頭**で、§4.2 の項目だけを渡して呼ぶ。`rangeOf`・`breakdownPage` の中に置く。`recordAnalytics` の `key` だけは既存の `isValidBreakdownKey` の検査の**後**に `assertUsableText('Analytics', { key })`
* **完了条件**：#22〜#26 が緑。既存の UseCase の結合テスト（`site`・`campaign`・`social`・`user`・`webhook`・`api-token`・`system-settings`・`plugin-settings`・`analytics`）と `data-api*.integration.test.ts` が緑
* **依存**：T2・T7
* **コミット**：`fix:`

#### T9. `recordAnalytics` の `siteId` の形（緑）

* **何をするか**：既存の検査（`metricDate` → … → `value`）と `key` の L2 の**後**、書き込みの前に `isUuidShape(input.siteId)` でなければ `ValidationError('Analytics', 'siteId', 'UUID の形で指定してください。')`（既存の検査の順序と既存テストの期待を変えない）
* **完了条件**：#27 が緑。`analytics.integration.test.ts`・`data-api.integration.test.ts` が緑
* **依存**：T8
* **コミット**：`fix:`

### G5. Plugin Store（L3。設計 §9.3・#30・#31）

#### T10. テストを書く（赤）

* **何をするか**：`plugin/store-text.integration.test.ts`。`store.integration.test.ts` の作り方（`plugin_store` の `plugin_id` に要る行を含む）を写す。#30 は前に入れた値が残ることも見る
* **完了条件**：#30 と #31 の `keys('k\u0000')` が赤。`keys('k\ud800')`・`setSecret`・絵文字は緑のまま
* **依存**：無し

#### T11. `store.ts` を直す（緑）

* **何をするか**：`serialize()` の `JSON.stringify` の前に `unusableTextDetailsOf(value)` が空でなければ `PluginStoreError(設計の文言, key)`。`keys(prefix)` は `prefix` が `containsNul` か `containsLoneSurrogate` なら `[]`
* **完了条件**：#30・#31 と `store.integration.test.ts`・`example-plugin.integration.test.ts`・配信 Plugin の結合テスト（`sns-*.integration.test.ts`）が緑
* **依存**：T2・T10
* **コミット**：`fix:`

### G6. Core が書く Plugin 由来・システム由来の文字列（L4。設計 §9.4・#32〜#36）

#### T12. テストを書く（赤）

* **何をするか**：`application/social/publish-text.integration.test.ts`（#32〜#35。`publish.integration.test.ts` の偽の publisher・`makePost`・`postRow` を写す）、`application/jobs/run-job-text.integration.test.ts`（#36。`run-job.integration.test.ts` を写す）
* **完了条件**：#32〜#34・#36 の NUL の条件が赤（`unrecorded`・記録の失敗）。#32 の片割れ・#35 は G2 の後なら緑
* **依存**：無し

#### T13. `publish.ts`・`run-job.ts` を直す（緑）

* **何をするか**：`safeText` の結果に `toStorableText` を掛ける。`redactAttemptResult` で `ok: true` の `externalId` にも掛ける（`externalUrl` は `decidePublishOutcome` が G2 の判定で捨てる）。`jobErrorText` を `truncateError(toStorableText(redactSecrets(…)))` に
* **完了条件**：#32〜#36 と `publish*.integration.test.ts`・`run-job.integration.test.ts`・`manual-handoff.integration.test.ts` が緑
* **依存**：T2・T4・T12
* **コミット**：`fix:`

### G7. B2（設計 §6.4・#37〜#39）

#### T14. テストを書く（赤）

* **何をするか**：`domain/analytics/analytics-date-range.test.ts`（#37）、`api/analytics-date-range.integration.test.ts` の #38（`rollup` は `job_runs` の行数を前後で数える）、`application/analytics/analytics-date-range.integration.test.ts` の #39
* **完了条件**：西暦 0 年の条件が赤、西暦 1 年・9999 年は緑
* **依存**：無し

#### T15. `isCalendarDate` に年の下限を足す（緑）

* **完了条件**：#37〜#39 と `analytics*.test.ts`・`rollup` のテストが緑
* **依存**：T14
* **コミット**：`fix:`

### G8. B3（設計 §6.5・#40〜#43）

#### T16. テストを書く（赤）

* **何をするか**：`domain/social/scheduled-at.test.ts`（#40）、`api/scheduled-at-range.integration.test.ts`（#41・#43。`social-post-create.integration.test.ts` の偽の publisher と Token を写す。数値の `scheduledAt` は JSON の数として送る）、`application/social/scheduled-at-range.integration.test.ts`（#42）
* **完了条件**：範囲外の条件が赤（500 / 201）。範囲内・`null`・#43 は緑
* **依存**：無し

#### T17. `isValidScheduledAt` と UseCase の検査を足す（緑）

* **何をするか**：`domain/social/social.ts` に定数と判定（**`Date.UTC(1, 0, 1)` で作らない**。定数はエポックミリ秒のリテラル）。`createSocialPost` は本文の検査（a）の直後、`updateSocialPost` は `scheduledAt` を指定したとき本文の検査の直後（どちらも L2 の後）。`schemas/social.ts` の作成・更新の `scheduledAt` に範囲を書いた `.describe()`
* **完了条件**：#40〜#43 と `social-post-*.integration.test.ts`・`social*.integration.test.ts`・`publish*.integration.test.ts` が緑
* **依存**：T8・T16
* **コミット**：`fix:`

### G9. B4 と OpenAPI（設計 §6.6・#44〜#46）

#### T18. テストを書く（赤）

* **何をするか**：`api/analytics-date-range.integration.test.ts` に #44（`job_runs` の行数を前後で数える。200 を見るときは `system.manage` を持つ利用者）、`application/analytics/analytics-date-range.integration.test.ts` に #45、`api/input-range-openapi.test.ts`（#46。`campaign-openapi.test.ts` の引き方を写す）
* **完了条件**：#44 の 36501 以上、#45、#46 の `maximum` と `ACCESS_LOG_PRUNE_MAX_DAYS` が赤。#46 の `scheduledAt` の `description` は G8 の後なら緑
* **依存**：無し

#### T19. 上限を足す（緑）

* **何をするか**：`ACCESS_LOG_PRUNE_MAX_DAYS`、ルートの `.max(…)`・`.describe()`、`pruneAccessLogs` の先頭で 1 以上・上限以下の整数でなければ `ValidationError('Analytics', 'pruneOlderThanDays', '36500以下で指定してください。')`
* **完了条件**：#44〜#46 と `openapi*.test.ts`・`analytics.integration.test.ts` が緑
* **依存**：T17（#46 の `description`）・T18
* **コミット**：`fix:`

### G10. N1〜N3（ユーザー裁定 3。設計 §9.2・#47〜#49）

#### T20. テストを書く（赤）

* **何をするか**：`plugin/data-api-record-and-status.integration.test.ts`（#47〜#49）。`data-api-text-input.integration.test.ts` と同じ利用者・Plugin の作り方。「何も書かれない」は行数で見る
* **完了条件**：#47〜#49 の誤りの条件が赤（`DatabaseError`）。列挙の値・`MAX_SAFE_INTEGER` は緑
* **依存**：無し

#### T21. 実装する（緑）

* **何をするか**：`createSite`・`updateSite` の `status`（指定したとき）を `isSiteStatus`、`createCampaign`・`updateCampaign` を `isCampaignStatus` で見て `ValidationError('Site' / 'Campaign', 'status', '状態の値が正しくありません。')`（L2 の後、既存の検査の並びの中）。
  `recordAnalytics` は T9 の形の検査の後、`Math.floor(value) > Number.MAX_SAFE_INTEGER` なら `ValidationError('Analytics', 'value', '9007199254740991以下の数値を指定してください。')`、
  書き込みのトランザクションの中で `siteRepository.findById(tx, siteId)` が `null` なら `ValidationError('Analytics', 'siteId', '存在しないWebサイトです。')`（`putPoint` の前）
* **完了条件**：#47〜#49 と既存の `site`・`campaign`・`analytics`・`data-api*` の結合テストが緑
* **依存**：T9・T20
* **コミット**：`fix:`

### G11. N4（ユーザー裁定 4。設計 §6.4・#50・#51）

#### T22. テストを書く（赤）

* **何をするか**：`domain/analytics/date-only-year.test.ts`（#50。ローカルの暦で `new Date(1, 0, 1)` は 1901 年になるので、`setFullYear(1, 0, 1)` で作る）、`api/date-only-year.integration.test.ts`（#51。`0001-01-01` の値は `analytics.record` で入れる）
* **完了条件**：年が 1000 未満の条件が赤。`2026-09-25` は緑
* **依存**：無し

#### T23. 実装する（緑）

* **何をするか**：`dateOnly`（`day.ts`）の年を `String(year).padStart(4, '0')`。`campaign-repository.ts` の `toDateOnly` は同じ実装の重複なので `dateOnly` の import に置き換える（Infrastructure → Domain の向きは許される）。§8 に記録する
* **完了条件**：#50・#51 と `campaign*.integration.test.ts`・`analytics*.test.ts` が緑
* **依存**：T22
* **コミット**：`fix:`

### G12. 公開 Plugin API の TSDoc と文書（設計 §9.5・§12・#53・#54）

#### T24. テストを書く（赤）

* **何をするか**：`plugin/text-input-docs-static-checks.test.ts`（#54）。`campaign-link-docs-static-checks.test.ts` の `read`・`docCommentBefore`・節の切り出しを写す
* **完了条件**：赤
* **依存**：無し

#### T25. TSDoc と文書を書く（緑）

* **何をするか**：設計 §9.5 の TSDoc（`store.ts`・`social.ts`・`data.ts`）、設計 §12 の `Plugin開発ガイド.md`・`SNS投稿API仕様.md`（§1.3・§1.4・§3.2・§4.4・§4.7・§5.2・§5.5・§9 の変更履歴。**片割れで 2xx だった要求が 422 になる動作の変更**を書く）。
  `010`・`045`・`037`・`040`・`042` 設計の追記・注記は検証の後（§3 の「検証」）
* **完了条件**：#54 と `plugin-api-docs-static-checks.test.ts`・`packages/plugin-api` のテストが緑。#53（`packages/plugin-api` の差分がコメントだけ、`plugins/` に差分なし）
* **依存**：T24・G3〜G11（書く振る舞いが実装されてから）
* **コミット**：`docs:`

### G13. 最終確認

#### T26. 最終確認

* **何をするか**：`pnpm generate:plugins` → `pnpm lint` → `pnpm format:check` → `pnpm typecheck` → `pnpm test`（全件。`DATABASE_URL=postgresql://torifune:torifune@localhost:21701/torifune_test`、CI のテスト用の `TORIFUNE_ENCRYPTION_KEY`）→ `pnpm build`
* **件数の突き合わせ**：`pnpm test` のファイル数と件数を G1 の前と後で記録する。**増えたファイルが 21**（§2 の vitest の新規）、増えた件数が新しいファイルだけを流した件数から、`campaign-link-input.integration.test.ts` から外した件数（NUL の行の `it.each` の展開分）を引いたものと合うこと
* **差分の確かめ（#53）**：基準は**親ブランチ `origin/feature/045-campaign-input-500`**。`git diff --name-only origin/feature/045-campaign-input-500..HEAD` が §2 の表のファイルと本件の文書だけ。
  `git diff origin/feature/045-campaign-input-500..HEAD -- packages/plugin-api` の追加・削除の行がコメント（`/**`・`*` で始まる）だけ、
  `-- migrations/ plugins/ packages/plugin-api/src/version.ts packages/plugin-api/src/index.ts apps/web/src/plugin/data-api.ts apps/web/src/application/authorization/use-case.ts` が空。
  既存のテストファイルの差分は `campaign-link-input.integration.test.ts` の 1 行（と §8 に記録した、許された期待の変更）だけ
* **E2E は流さない**：画面の差分が無く（`app/**`・`ui/**` の差分が空であることを上の差分で確かめる）、設計 §10.14 のとおり D の条件が無い。
  L1 は画面の保存が通る `route.ts` を変えるが、C のテストが本物の Route Handler をセッション・CSRF の経路で叩いて確かめる（#10・#13〜#17）
* **完了条件**：#1〜#54 が全件緑、既存テストが（許された変更を除き）無変更で緑
* **依存**：T25

#### 検証（プランの外の工程。順序だけを書く）

T26 の後、**`spec-verifier`・`security-reviewer`・`boundary-guardian` を互いの結果を見ずに回す**（共有の API 層（`route.ts`）と公開 Plugin API の振る舞いを変えるため 3 つとも要る）。見る範囲は `653e211..HEAD`。

* **`spec-verifier`**：#1〜#54、設計 §10.13（既存テストの変更の範囲）、設計 §1.2 の「500 になったもの」の全行が 500 でなくなったこと、§4.2 の 45 組と実装の突き合わせ、§6.8 の「Plugin の `validate()` に片割れが届かない」こと（#21）
* **`security-reviewer`**：設計 §1.2 の入力（NUL・片割れ）を本物の Route Handler に投げて 500 が無いこと、`details`・`message`・ログに送った値が載らないこと、認可が検査より先であること（#10・#23・#28・#43）、
  認証の要らない口の 422 が利用者の有無・キーの当たり・セットアップの状態を漏らさないこと（#13〜#16）、`toStorableText` が秘匿の後であること（設計 §13 の 4）、ReDoS の無い正規表現であること、N2 の存在の確かめが書き込みと同じトランザクションであること
* **`boundary-guardian`**：Data API（片割れが成功から `ValidationError` に変わる動作の変更を含む）・Store・配信の戻り値の振る舞いの変更（設計 §9.5）の判断と記録、`packages/plugin-api` の差分がコメントだけであること、
  `plugins/` を変えずに Plugin の検査を残す判断（設計 §6.8）、Domain の `text.ts` が他の層に依存しないこと、L2 を `defineUseCase` に置かなかった判断（設計 §4.3）、HTTP v1 のまま出す判断（設計 §6.7）

検証の後、設計 §12 の書き戻し（`010` 設計 §9 の追記・`045`・`037`・`040`・`042` 設計の注記）と本件の検証レポートを書く。

## 4. テスト計画

種別は設計 §10 の記号に従う。**A = 単体**（vitest、DB なし）、**B = 結合**（vitest、DB あり）、**C = API**（vitest、Route Handler を直接呼ぶ。DB あり）、**E = 静的検査**（vitest またはコマンド）。D（E2E）は無い。

| # | 受け入れ条件（要旨） | 種別 | テストファイル |
| --- | --- | --- | --- |
| 1 | `containsNul` の真偽 | A | `apps/web/src/domain/text.test.ts` |
| 2 | `containsLoneSurrogate` の真偽（先頭・途中・末尾・逆順・絵文字の後ろ） | A | 同上 |
| 3 | `unusableTextDetailsOf` の最上位の項目名・`_`・文言の順・重複なし | A | 同上 |
| 4 | `toStorableText` の置き換えと長さ | A | 同上 |
| 5 | `NUL_MESSAGE`・`LONE_SURROGATE_MESSAGE` | A | 同上 |
| 6 | URL の判定が NUL を偽に | A | `apps/web/src/domain/social/external-url-nul.test.ts` |
| 7 | 表 H の 36 件 × NUL・片割れ → 422、キー 1 つ、書かれない | C | `apps/web/src/api/nul-input.integration.test.ts` |
| 8 | クエリ 7 か所 → 422 | C | 同上 |
| 9 | 複数・同じ項目に両方・入れ子・`siteIds`・最上位のキー・Zod を走らせない | C | 同上 |
| 10 | 401・403・`CSRF_FAILED` が先 | C | 同上 |
| 11 | 応答に値が無い・500 のログが無い | C | 同上 |
| 12 | パスの `{id}` は 404、`/auth/callback` は 302 のまま | C | 同上 |
| 13 | `/auth/login` の NUL・片割れ → 422、存在の有無で同じ本文、記録なし | C | `apps/web/src/api/nul-public-input.integration.test.ts` |
| 14 | `/auth/password-reset/*` の NUL・片割れ → 422 | C | 同上 |
| 15 | `/setup` の NUL・片割れ → 422（開いていても閉じていても） | C | 同上 |
| 16 | `/collect` の NUL・片割れ → 422、記録なし | C | 同上 |
| 17 | `/auth/authorize?returnTo=` の NUL → 422 | C | 同上 |
| 18 | `jsonb` の項目の片割れ → 422、書かれない | C | `apps/web/src/api/lone-surrogate-input.integration.test.ts` |
| 19 | `/settings`・Plugin の設定の片割れ → 422、前の値のまま | C | 同上 |
| 20 | いま 2xx の `text` 列の片割れ（`/sites`・`/webhooks`・本文・`externalRef`）→ 422 | C | 同上 |
| 21 | 配信 Plugin の `validate()` に届かず Core の文言、`link` のキー、絵文字は 201 | C | 同上 |
| 22 | §4.2 の 45 組 × NUL・片割れ → `ValidationError` | B | `apps/web/src/application/text-input.integration.test.ts` |
| 23 | 認可が先（`ForbiddenError`） | B | 同上 |
| 24 | 入れ子の片割れ（`providerOptions`・`values`）→ `ValidationError`、絵文字は成功 | B | 同上 |
| 25 | `get*` の `NotFoundError`・`keys`・`key` の NUL の既存の誤りは変わらない | B | 同上 |
| 26 | Data API の 15 か所 × NUL・片割れ、`analytics.record` の `key` の片割れ → `ValidationError` | B | `apps/web/src/plugin/data-api-text-input.integration.test.ts` |
| 27 | `analytics.record` の `siteId` の形 | B | 同上 |
| 28 | `PluginPermissionError`・`ForbiddenError` が先 | B | 同上 |
| 29 | `get()` の `null`・一覧の `PluginDataInputError` は変わらない | B | 同上 |
| 30 | `store.set` の NUL・片割れ → `PluginStoreError`、前の値が残る | B | `apps/web/src/plugin/store-text.integration.test.ts` |
| 31 | `keys` の NUL・片割れ → `[]`、`setSecret`・絵文字は変わらない | B | 同上 |
| 32 | `externalId` の NUL・片割れ → `published`・U+FFFD | B | `apps/web/src/application/social/publish-text.integration.test.ts` |
| 33 | `externalUrl` の NUL → `published`・`NULL` | B | 同上 |
| 34 | `reason`・例外の文言の NUL → `failed`・NUL なし | B | 同上 |
| 35 | `manual()` の NUL を含む URL → `invalid_url` | B | 同上 |
| 36 | ジョブの例外の文言の NUL → `job_runs` に記録 | B | `apps/web/src/application/jobs/run-job-text.integration.test.ts` |
| 37 | `isValidRange` の西暦 0 年 | A | `apps/web/src/domain/analytics/analytics-date-range.test.ts` |
| 38 | `/analytics`・`breakdown`・`rollup` の西暦 0 年 → 422 `to` | C | `apps/web/src/api/analytics-date-range.integration.test.ts` |
| 39 | Data API の西暦 0 年 → `ValidationError` | B | `apps/web/src/application/analytics/analytics-date-range.integration.test.ts` |
| 40 | `isValidScheduledAt` の境目・定数 | A | `apps/web/src/domain/social/scheduled-at.test.ts` |
| 41 | `scheduledAt` の範囲外 → 422、範囲内 → 201 / 200 | C | `apps/web/src/api/scheduled-at-range.integration.test.ts` |
| 42 | UseCase の `scheduledAt` の範囲外 → `ValidationError` | B | `apps/web/src/application/social/scheduled-at-range.integration.test.ts` |
| 43 | `scheduledAt` の 401・403 が先 | C | `apps/web/src/api/scheduled-at-range.integration.test.ts` |
| 44 | `pruneOlderThanDays` の 36501 以上 → 422、ジョブが走らない | C | `apps/web/src/api/analytics-date-range.integration.test.ts` |
| 45 | `pruneAccessLogs` の上限 | B | `apps/web/src/application/analytics/analytics-date-range.integration.test.ts` |
| 46 | OpenAPI の `maximum`・`description`・定数 | A | `apps/web/src/api/input-range-openapi.test.ts` |
| 47 | Data API の列挙外の `status` → `ValidationError` | B | `apps/web/src/plugin/data-api-record-and-status.integration.test.ts` |
| 48 | `analytics.record` の存在しない `siteId` → `ValidationError` | B | 同上 |
| 49 | `analytics.record` の `value` の上限 | B | 同上 |
| 50 | `dateOnly` の年が 4 桁 | A | `apps/web/src/domain/analytics/date-only-year.test.ts` |
| 51 | `metricDate`・`startsOn` / `endsOn` の年が 4 桁 | C | `apps/web/src/api/date-only-year.integration.test.ts` |
| 52 | `typecheck`・`lint`・`format:check` | E | コマンド（T26） |
| 53 | `packages/plugin-api` はコメントだけ、`version.ts`・`index.ts`・`migrations/`・`plugins/` に差分なし | E | コマンド（T26 の差分の確かめ） |
| 54 | TSDoc・ガイド・SNS 投稿 API 仕様の記述 | E | `apps/web/src/plugin/text-input-docs-static-checks.test.ts` |

**件数の確認**：受け入れ条件 54 件 = A 10（#1〜#6・#37・#40・#46・#50）+ B 21（#22〜#36・#39・#42・#45・#47〜#49）+ C 20（#7〜#21・#38・#41・#43・#44・#51）+ E 3（#52〜#54）。表の行も 54。条件付きは無い。
新しいテストファイルは vitest **21** = A 6（`domain/text`・`domain/social/external-url-nul`・`domain/analytics/analytics-date-range`・`domain/social/scheduled-at`・`api/input-range-openapi`・`domain/analytics/date-only-year`）+
B 8（`application/text-input`・`plugin/data-api-text-input`・`plugin/store-text`・`application/social/publish-text`・`application/jobs/run-job-text`・`application/analytics/analytics-date-range`・`application/social/scheduled-at-range`・`plugin/data-api-record-and-status`）+
C 6（`api/nul-input`・`api/nul-public-input`・`api/lone-surrogate-input`・`api/analytics-date-range`・`api/scheduled-at-range`・`api/date-only-year`）+ E 1（`plugin/text-input-docs-static-checks`）。§2 の表の新規のテストの行も 21。

**権限系**：403・401 は #10（HTTP の本文・クエリ）・#43（`scheduledAt`）、UseCase は #23、Data API は #28。認証の要らない口は Permission を持たない（#13〜#17 は応答が利用者の有無・状態に依存しないことを見る）。
**他の利用者のデータへの到達**：サイト・SNS 投稿・キャンペーンは利用者ごとの所有を持たず（設計 §1.2.6）、本件は Permission も到達の範囲も変えないので新しいテストは足さない（既存の権限のテストが担保する）。

## 5. マイグレーション

無し。既存の行（U+FFFD を含む値）は変えない。

## 6. 影響範囲

* **HTTP API**：設計 §6.7。NUL で 500・409 だった 45 件が 422 に、NUL で 2xx・401・400 だったものが 422 に、**片割れで 2xx（U+FFFD で保存）だったものが 422 に**（動作の変更。ユーザー裁定 2）、
  片割れの `jsonb` の 500 が 422 に、配信 Plugin が返していた片割れの 422 の文言が Core のものに（`link` はキーも）。西暦 0 年・`scheduledAt` の範囲外・`pruneOlderThanDays` の 36501 以上が 422 に。年が 1000 未満の日付の応答が 4 桁に。OpenAPI に `maximum`・`description` が増える。**v1 のまま**
* **Data API**：NUL・`analytics.record` の `siteId`・西暦 0 年・N1〜N3 の `DatabaseError` が `ValidationError` に、片割れが成功から `ValidationError` に（設計 §9.2・§9.5）。型は変えない
* **Plugin Store**：`set` の `DatabaseError` が `PluginStoreError` に、`keys` の NUL が `[]` に（設計 §9.3）
* **配信**：NUL を含む戻り値でも結果が記録される。`manual()` の NUL を含む URL が `invalid_url` に（設計 §9.4）。同梱の配信 Plugin は変えず、その片割れの検査には Core の経路から届かなくなる（設計 §6.8）
* **画面**：変わらない（保存の 422 の `details` のキーは既存の欄の名前）
* **公開 Plugin API の型**：変わらない。**破壊的変更は無い**（振る舞いの変更は設計 §9.5 で判断済み）
* **既存テスト**：1 行（設計 §10.13）。片割れを 2xx で保存する前提のものは検索で見つかっていない

## 7. リスクと注意点

1. **裁定は出ている（2026-09-25、ユーザー裁定 1〜4）。** 初版で「裁定待ち」としていた G3・G4 のコミット前の確かめは要らない。裁定 2 で片割れを断るので、L1・L2 に**置き換えの処理を作らない**（初版の `withWellFormedText` は無い）
2. **L1 の位置**：`requirePermission` より前に置くと、権限の無い要求に入力の誤りを返す（#10 で落ちる）。Zod より後に置くと、#9 の「Zod を走らせない」と `siteIds` の文言が崩れる
3. **#21 の順序**：L1 と L2 は配信 Plugin の `validate()`（`checkPublisherLimits`）より先。L2 を `createSocialPost` の既存の検査の後ろに置くと、UseCase を直接呼ぶ経路で Plugin の文言が先に返る（#22 の `body`・`link` で落ちる）
4. **正規表現**：後読み（`(?<!…)`）は Node 22 で使える。`u` フラグを付けると単独のサロゲートに一致しなくなるので**付けない**。入力の長さに対して線形（量指定子の入れ子が無い）。
   `lib` を `ES2024` に上げて `isWellFormed` を使うことはしない（型の変更が全体に及ぶ。`037` の先例）
5. **テストのソースの文字**：U+FFFD と絵文字は `String.fromCodePoint` で作り、片割れは `\ud800` のエスケープで書く。ソースに U+FFFD の文字そのものを書くと、置き換えの結果と区別がつかない
6. **#7 の表の作り方**：36 件の本文は「通る本文」を土台に 1 か所だけ値を足す。土台が他の理由で 422 になると検査を確かめていないのと同じになるので、**土台だけを送ると 2xx になること**を同じファイルの 1 件目で確かめる
7. **`/setup` のテスト**：利用者が 0 人でないと開かない。使い捨ての DB でも、他のテストが作った利用者を各ケースの前に消す（外部キーの順：`user_roles` → `auth_audit_logs` → `sessions` → `users`）
8. **#13 の「記録なし」**：`login_attempts` と `auth_audit_logs` の行数を前後で比べる。ルートの Rate Limit（1 分 30 回）に当たらないよう、1 ファイルの `/auth/login` の呼び出しは 30 回未満にする（`X-Forwarded-For` をケースごとに変えてもよい）
9. **#36 のジョブ名**：`job_runs.job_name` に入る名前は `run-job.integration.test.ts` の作り方に従う（`JobName` の型）
10. **#44 と `system.manage`**：36500 以下の 200 を見るときは `system.manage` を持つ利用者で叩く（持たないと集計の後に 403）
11. **N2 の順序**：`siteId` の存在の確かめは `putPoint` と同じトランザクションの中、形の検査（T9）の後。形の誤りで DB を引かない

## 8. 設計書に対する補足（実装時に決めたこと・テストの読み方の記録）

### 8.1 テスト作成者の読み方（1〜7）

**（2026-09-25 implementer）テスト作成者の読み方 1〜7 は、受け渡しのファイル（本プラン・設計書・テストのコミット `fd7fa0d..724b09e` のメッセージとテストの中のコメント）に書かれていなかった。**
実装者の手元には届いていないので、ここには写していない。オーケストレーターの手元にあれば、この節に書き足すこと。

**（2026-09-25 オーケストレーターが書き足す。テスト作成者の報告から）**

1. 「何も書かない・変わらない」は、対象の表の `SELECT *` を並べ替えて前後で比べる。#22 の `social.post.create` の `externalRef` は API Token の文脈（`buildApiTokenContext`）から呼ぶ
2. #22 では設計 §4.2 の表どおり `resource` も見る（`Plugin の設定` を含む）。`savePluginSettings` の `values` のキーの NUL も `field === 'values'` とした（§4.2 に「キーと値」とあるため）
3. 文言まで見る箇所：#27 `UUID の形で指定してください。`、#47 `状態の値が正しくありません。`、#48 `存在しないWebサイトです。`、#49 `9007199254740991以下の数値を指定してください。`、#39 `日付の形式が不正です。`、#42 範囲の文言
4. 設計から足したテスト：#32 同じ投稿で 2 回目の配信を走らせても `publish()` が 1 回しか呼ばれない（二重投稿にならない）。#34 片割れの `reason` と、NUL を含む資格情報の値を `reason` に混ぜても平文で残らないこと（§13 の 4：置き換えは伏せ字の後）。#9 `csrfToken` の片割れで `details.csrfToken`、`url` が欠けていても `name` だけが返ること。#44 `system.manage` を持たない Token でも 36501 は 422。#12 `GET /campaigns/{id}`。#13 `loginId` の片割れ
5. #42 の境目ちょうどの対照は「作成できた」ことだけを見る（年 0001 の読み戻しは Node の地方平均時のオフセットに左右されうるため。境目ちょうどの判定は #40 が見る）
6. #36 は `error` が `'x'` と U+FFFD の 2 文字ちょうどになることを見る
7. #20 の「`POST /webhooks` は `audit_logs` も増えない」は 046 の前も通る（監査ログの記録が黙って失敗していたため）
実装の側でテストから読み取ったことは 8.2 に書いた（テストの期待と実装が食い違ったものは無い。8.4 の 1 件を除く）。

### 8.2 実装時に決めたこと

1. **L1 のクエリの集め方**：宣言したルートだけ `searchParams` を 1 回集め（`rawQuery`）、使えない文字の検査と Zod の両方に渡す。宣言していないルート（`/auth/callback`）は集めない（設計 §6.2）。
   本文とクエリの誤りは `mergeUnusableTextDetails` で本文 → クエリの順にまとめ、同じキーは文言を重ねない
2. **`unusableTextDetailsOf`**：最上位がプレーンなオブジェクトのとき、キーごとに「キー自体（→ `_`）」「値の中（→ そのキー）」を見る。キーの順は本文の順（`_` は最初に出た位置）。
   入れ子はプレーンなオブジェクトと配列だけをたどる（`Date`・`Uint8Array` は見ない）。文言の順は出た順によらず NUL → 片割れ
3. **ソースに NUL・U+FFFD を書かない**：`domain/text.ts` の NUL と U+FFFD は `String.fromCharCode(0)` / `String.fromCharCode(0xfffd)` で作る
   （正規表現の `\u0000` は ESLint の `no-control-regex` に当たるため、NUL の置き換えは `replaceAll`）。片割れの正規表現だけは `\uD800` などのエスケープで書く（`u` フラグなし）
4. **L2 の位置**：ハンドラの先頭。ただし `user.create` / `user.update` / `apiToken.create` は既存の `requireAuthenticated`（認証の確かめで、入力の検査ではない）の直後。
   `plugin.settings.save` は `requireSettings`（Plugin の有無）より前。`rangeOf`・`breakdownPage` は先頭（`assertRange` より前）。
   `analytics.record` の `key` は `isValidBreakdownKey` の直後、`value` の検査の前
5. **N1 の位置**：作成は L2 と既存の `name` / `url`（サイト）・`name`・期間（キャンペーン）の検査の直後。更新は `status` を指定したときだけ、既存の `name`（と `url`）の検査の直後
6. **N2・N3・`siteId` の形の順**：`value` の下限 → `siteId` の形（`isUuidShape`）→ `value` の上限（`Math.floor` の後）→ トランザクションの中で `siteRepository.findById` → `putPoint`。
   形の誤りでは DB を引かない（§7 の 11）。書き込む値は切り捨てた `value` を 1 回だけ計算して使う
7. **B3**：`assertScheduledAtInRange` を `social-use-cases.ts` に置き、作成は本文の検査（a）の直後（`null` でないとき）、更新は本文の検査の直後（`undefined`・`null` でないとき）
8. **B4 の文言**：`pruneAccessLogs` は 36501 以上を `36500以下で指定してください。`（Zod の `.max` と同じ文言）、**1 未満・整数でない値は `1以上の整数で指定してください。`** にした。
   T19 の書き方は両方に `36500以下で…` を当てるようにも読めるが、0 や 1.5 に「36500以下で」と返すのは直し方を誤らせる。テスト（#45）は `field` だけを見る。HTTP は従来どおり Zod が下限を先に断るので、応答は変わらない
9. **L3**：`serialize()` で JSON にした後、大きさの検査の**後**に `JSON.parse(json)` を `unusableTextDetailsOf` に掛ける。JSON にした形で見るので、`toJSON` を持つ値やクラスの値の中の文字列も漏れない。
   循環する値は `JSON.stringify` が先に断る（`collect` に循環の守りを持たせずに済む）。`setSecret` は暗号文だけを `serialize` に渡すので影響しない
10. **L4**：`safeText` を `toStorableText(redactCredentialValues(redactSecrets(text), values))` に（秘匿の後・`normalizedVerdict` の切り詰めの前）。
    `ok: true` の `externalId` は `redactAttemptResult` で `toStorableText` だけを掛ける（秘匿は従来どおり掛けない）。`jobErrorText` は `truncateError(toStorableText(redactSecrets(…)))`
11. **N4**：`campaign-repository.ts` の `toDateOnly` は `const toDateOnly = dateOnly`（`domain/analytics/day.ts`）にした（T23。同じ実装を 2 つ持たない）
12. **OpenAPI**：`pruneOlderThanDays` の `.describe()` に範囲（1〜36500）と、消すには `system.manage` が要ること（既存の振る舞い）を書いた。`scheduledAt` は作成・更新で共有する `scheduledAtSchema` に `.describe()`
13. **文書**：オーケストレーターの指示で、設計 §12 の `010` §9 の追記と `045`・`037`・`040`・`042` の注記を検証の前のこの往復で書いた（§3 の「検証」では検証の後としていた）。検証で直すことがあれば同じ往復で追いつかせる

### 8.3 コミットの分け方

テストのコミット（`fd7fa0d..724b09e`）の後、実装を 5 つ（L1・L2 と範囲・Store・L4・アナリティクス）、文書を 1 つに分けた。
L2 のコミットは `social.ts`・`social-use-cases.ts` などを G2・G4・G8・G10 で共有するので、グループごとには分けずに 1 つにまとめた。

### 8.4 テストと設計の食い違い（報告して止めたもの）

* **`application/social/publish-text.integration.test.ts` の #35 の 2 件は、テストの準備の誤りで失敗する**（実装では通せない。テストは書き換えていない）。
  #35 の `useManualPublisher()` は `credentialFields` に `identifier` だけを宣言するが、`accountFor()` は `credentials: { identifier, appPassword }` を送る。
  `createSocialAccount` の既存の検査（`validateCredentialAgainstFields`。宣言外のキーを断る。`035` からの規則）が `appPassword は指定できません（定義に無い項目）。` の `ValidationError` を投げ、
  `resolveManualHandoff` まで届かない（NUL の URL の件と、NUL の無い URL の対照の件の両方）。設計 §10.8 #35 の期待（`invalid_url`）と実装（`isValidExternalHttpsUrl` の NUL の判定）は合っている。
  直すなら #35 で宣言と合う資格情報を渡す（テスト側の修正）。オーケストレーターへ報告した
  → **（2026-09-25 解消）** オーケストレーターがテスト側で `useManualPublisher()` の `credentialFields` に `appPassword` を足し、`usePublisher()` と揃えた。#35 の 2 件を含め、ファイルの 20 件が通る

### 8.5 件数

`pnpm test`（CI と同じ `DATABASE_URL`・`TORIFUNE_ENCRYPTION_KEY`）：**252 ファイル / 10,665 件**。うち失敗は 8.4 の 2 件だけ（`publish-text.integration.test.ts`）。
`pnpm typecheck`・`pnpm lint`・`pnpm format:check`・`pnpm build` は通過。#53 の差分（基準 `origin/feature/045-campaign-input-500`）：`migrations/`・`plugins/`・`version.ts`・`index.ts`・`data-api.ts`・`use-case.ts`・`ui/` は空、
`packages/plugin-api` はコメントの行だけ、既存のテストファイルの差分は `campaign-link-input.integration.test.ts` の 1 行だけ、`app/` は `api/v1/analytics/rollup/route.ts` だけ（画面の差分なし）。

**E2E**：§3 の T26 では流さないとしていたが、L1 がすべての API のルートを通るので、オーケストレーターの指示で全件を流した。
`039` 実装プラン §8 の 35 の手順（`git archive HEAD`（`06dd372`）を `mcr.microsoft.com/playwright:v1.62.1-noble` のコンテナへ展開し、`pnpm install --frozen-lockfile` → `pnpm generate:plugins` → `pnpm migrate` → `pnpm test:e2e`。
DB は作業機の postgres-test を `host.docker.internal:21701` で使う。`CI` は立てず再試行なし。`--rm`）。**496 件すべて通過**（3.7 分）。既存の E2E は変えていない。

### 8.6 検証の指摘への処置（2026-09-25）

`spec-verifier`・`security-reviewer`・`boundary-guardian` の指摘を、オーケストレーターがすべて「直す（または記録する）」と決めた。どれも同じ往復で処置した。
テストは**設計の意図から先に書き、落ちることを確かめてから**実装した（テストを弱めた箇所は無い）。

| 指摘 | 決定 | 処置 | テスト（赤 → 緑） | コミット |
| --- | --- | --- | --- | --- |
| **M1**（046 が入れた退行）：`Object.prototype` の名前（`__proto__`・`constructor`・`toString`・`valueOf`・`hasOwnProperty`）の項目に NUL・片割れがあると、`domain/text.ts` の `mergeInto` と `api/route.ts` の `mergeUnusableTextDetails` が `details[key] ?? []` で継承した関数を拾って `TypeError` → 認証の要らない `/collect`・`/auth/login` を含むすべての JSON の口と `GET /auth/authorize?constructor=%00` が 500。Store の `set(key, { toString: 'x\u0000' })` も `TypeError` | 直す | 項目名 → 文言を `Map` に積み、`Object.fromEntries` で**自分のプロパティ**として返す（`__proto__` も原型を変えずにキーになる）。クエリも `Object.fromEntries(searchParams)` で集め、`?__proto__=` が代入で消えて検査から漏れないようにした | `domain/text.test.ts` の M1（最上位の 5 名前 × NUL・片割れ、入れ子、使えない文字なし、`__proto__` が自分のプロパティ）。`api/unusable-text-edge-input.integration.test.ts` の M1（`/auth/login`・`/collect`・`POST /sites` の本文 5 名前 × 2 通り → 422・`details` がその名前 1 つ・500 のログなし。`/auth/authorize`・`GET /sites` のクエリ 5 名前 → 422）。`plugin/store-text.integration.test.ts` の M1（5 名前 → `PluginStoreError`、前の値が残る）。**赤**：`TypeError: current.includes is not a function`（直す前は本文・クエリ・Store の各件が 500・`TypeError`。`?__proto__=%00` は 422 にならなかった） | `c1697de` |
| **M2**（046 が入れた退行）：`collect` が再帰で、5000 段ほどの入れ子の JSON（約 10KB。認証なしで送れる）で `RangeError` → 500 | 直す | 明示のスタックでたどる。同じオブジェクト・配列は 1 回しか見ない（線形。循環でも止まる）。項目の順（本文の順）と文言の順（NUL → 片割れ）は変えない | `domain/text.test.ts` の M2（1 万段の配列・オブジェクト・キー、深さの違う 2 か所、最上位の配列 → `_`、使えない文字なし → 空、項目の順）。API の M2（`/auth/login` の `loginId`・`/collect` の `key`・`POST /sites` の `name` に 1 万段の配列・オブジェクト：底に NUL → 422 NUL の文言、無し → 従来どおり Zod の 422、500 のログなし）。Store の M2（`JSON.stringify` が書ける深さの 9 割（実行環境で測る）の底に NUL・片割れ → `PluginStoreError`、1 万段 → `PluginStoreError`）。**赤**：`RangeError: Maximum call stack size exceeded` | `c1697de` |
| **security L1**：`publish.ts` の配信直前の再検査（`precheck`）が、`validate()` の `field`・`message`・例外の文言、`limits` 経路の `label` を `redactSecrets` だけ通して理由文に埋め込み、`toStorableText` を通していなかった。NUL で `recordOutcome` が失敗 → `unrecorded` → 次の回で「中断」 | 直す | `precheckText`（`toStorableText(redactSecrets(…))`。伏せてから置き換える、`safeText` と同じ順）を 3 か所に掛けた。`label` は `checkPublisherLimits` が `message` に埋め込むので同じ関数を通る | `application/social/publish-text.integration.test.ts` の「security L1」（`message` の NUL・`field` の片割れ・例外の NUL・`label` の NUL × `failed` で記録（`unrecorded: 0`）／`failure_reason` が U+FFFD を含み NUL・片割れを含まない／着手印が残らず `publish()` を呼ばない）。**赤**：NUL の 3 通り × 3 件が `unrecorded: 1`・`failure_reason` が空・着手印が残る（片割れの 1 通りは `text` 列が U+FFFD にするので直す前から緑。守りとして残す） | `0a617f1` |
| **security L2**：L1 の 422 の `details` が項目の数だけキーを返し、認証の要らない口で応答を膨らませられる | 直す（先頭の 50 個） | `api/route.ts` の `UNUSABLE_TEXT_DETAIL_KEYS_MAX = 50`。本文 → クエリの順で 51 個目から入れない。設計 §6.2 に追記 | API の「security L2」（`/collect` の本文 60 項目 → 先頭の 50 項目・各文言、ちょうど 50 → 50、`/auth/authorize` のクエリ 60 個 → 先頭の 50 個）。**赤**：60 個すべてが返った | `3dd4e7a` |
| **spec 軽微 1**：#11 が 500 のログしか見ておらず、422 を返す前に送った値をログへ書く変異（M47）が生き残る | テストを足す | — | `api/nul-input.integration.test.ts` の #11 に「どのレベルのログにも `marker-046` が無い」（表 H の 72 件）。`route.ts` に送った本文を `log.debug` で書く変更を入れると **72 件が落ちる**ことを確かめてから戻した | `c16dc6b` |
| **spec 設計の注記**：`createSocialAccount` / `updateSocialAccount` の `status`、`createSocialPost` の `status`・`deliveryMode`、`recordAnalytics` の `source` は UseCase を直接呼ぶと NUL で `DatabaseError` | 記録だけ | 設計 §4.2 の末尾・§13 の 3 に追記（外から届く経路が無い：HTTP は Zod の `enum` と L1、Data API は `source` を固定） | — | 文書のコミット |
| **boundary Low 1**：NUL・片割れの `loginId` / `password` が `authenticate()` に届かなくなったことが書かれていない | 記録する | `010` 設計 §9 の 046 の追記・046 設計 §9.5 の表・`Plugin開発ガイド.md` §9 の Authentication Provider に書いた（契約は変わらない） | — | 文書のコミット |
| **boundary Low 2**：`PluginDataApi` の TSDoc が ID の引数を除いていない | 記録する | `packages/plugin-api/src/data.ts` の TSDoc と `Plugin開発ガイド.md` §4 に「ID の引数はこの規則の外（`get(id)` は `null`、一覧の ID の絞り込みは `PluginDataInputError`）」。`packages/plugin-api` の差分はコメントの 2 行だけ | — | 文書のコミット |
| **boundary Info**：`マニュアル/アクセス解析設置.md` の「成功も失敗も `204`」が不正確（形の誤りと NUL・片割れは 422）。`SNS投稿API仕様.md` §1.3 の「クエリの値」がクエリの名前（`_` で返る）を落としている | 直す | 前者は「キーの当たり外れは 204、要求の形の誤りと NUL・片割れは 422」に。後者は「クエリ（値と名前）」「名前そのものに含むときは `_`」「先頭の 50 個まで」を書き、§9 の変更履歴に 1 行。046 設計 §6.1 の同じ記述も直した | — | 文書のコミット |

**実装で決めたこと**：M2 のたどり方は `seen`（`Set<object>`）で同じオブジェクトを 1 回だけ見る。HTTP の本文は `JSON.parse` の木なので効かないが、UseCase を直接呼ぶ経路（L2）で共有・循環する値を渡されても止まる。
`unusableTextDetailsOf` の返り値は `Object.fromEntries` の通常のオブジェクトのまま（既存のテストの `toEqual` と `ValidationError.details` の型を変えない）。
Store の M2 の深さを実行環境で測るのは、`JSON.stringify` の書ける深さ（この環境で約 4,800 段）が再帰の `collect` の限界（約 3,400 段）より深く、その間でしか直す前の `RangeError` が観測できないため。

**件数**：`pnpm test`（CI と同じ `DATABASE_URL`・`TORIFUNE_ENCRYPTION_KEY`）**253 ファイル / 10,935 件、すべて通過**（`70c563d` の 252 ファイル / 10,665 件に、新しいファイル 1（133 件）と既存の 4 ファイルへの追加 137 件（`domain/text` 33・`store-text` 20・`publish-text` 12・`nul-input` 72）を足した数と一致）。
`pnpm typecheck`・`pnpm lint`・`pnpm format:check`・`pnpm build` は通過。`plugins/`・`migrations/` の差分なし、`packages/plugin-api` はコメントの 2 行だけ。
ソースに NUL・対になっていないサロゲート・U+FFFD の文字そのものが無いことを、変えたファイルのバイト列（`\x00`・`EF BF BD`・`ED A0`〜`ED BF`）で確かめた。

**E2E**：`api/route.ts` を再び変えたので、処置のコミットの後に全件を流した。§8.5 と同じ手順（`039` 実装プラン §8 の 35。`git archive HEAD`（`005f483`）を
`mcr.microsoft.com/playwright:v1.62.1-noble` のコンテナへ展開し、`pnpm install --frozen-lockfile` → `pnpm generate:plugins` → `pnpm migrate` → `pnpm test:e2e`。
DB は作業機の postgres-test を `host.docker.internal:21701` で使う。`CI` は立てず再試行なし。`--rm`。ほかの重い処理と重ねない）。**496 件すべて通過**（3.7 分）。既存の E2E は変えていない。

#### 2 回目の検証の指摘への処置（2026-09-25）

1 回目の処置（上の表）の後の検証で、L1 の外に送った側が決める形で投げる箇所が見つかった。オーケストレーターがすべて「直す（または記録する）」と決めた。
テストは 1 回目と同じく**先に書き、落ちることを確かめてから**実装した（テストを弱めた箇所は無い）。

| 指摘 | 決定 | 処置 | テスト（赤 → 緑） | コミット |
| --- | --- | --- | --- | --- |
| **N1**（中。認証なしで 500）：`api/route.ts` の CSRF の段が本文の `csrfToken` を `String()` にしてから比べていた。`{"csrfToken":{"toString":1}}` で `TypeError`、1 万段の配列で `Array.prototype.join` の `RangeError` → `/auth/login` を含むすべての CSRF を検証する口が 500・`unhandled error in route` | 直す | 本文の `csrfToken` は `typeof === 'string'` のときだけ使う。**文字列でなければ `x-csrf-token` ヘッダの有無にかかわらず 403 `CSRF_FAILED`**（ほかの型を文字列にしない）。`in` を `Object.hasOwn` に。設計 §6.2 の順序に追記 | `api/unusable-text-edge-input.integration.test.ts` の N1（`/auth/login`・`POST /sites`、Bearer なし × 9 通りの値（`toString` が関数でないオブジェクト・`toString` と `valueOf`・空のオブジェクト・1 万段の配列・1 万段のオブジェクト・`["<トークン>"]`・数値・真偽値・`null`）× 本文だけで送る → 403 `CSRF_FAILED` ／ エラーのログなし ／ ヘッダが正しくても 403。対照 2 件：同じ文字列 → 403 にならない、違う文字列 → 403）。**赤**：`TypeError`・`RangeError` の 500 と `unhandled error in route`、`["<トークン>"]` は CSRF を通って 401、ヘッダが正しいと値によって 500 / 422 / 401 | `b14d3c0` |
| **N2**（低。認証ありで 500）：`api/schemas/social.ts` の `providerOptions` の `refine` が大きさを測る前に `JSON.stringify` を投げっぱなしにしていた。約 5,000 段の入れ子で `RangeError` → 500 | 直す | `try` / `catch` で囲み、書けない値は**上限を超えたものとして従来の 422**（キー `providerOptions`・文言 `JSON にして4096バイト以内にしてください。`） | `api/social-post-create.integration.test.ts` の N2（1 万段の配列・オブジェクト → 422・その文言だけ ／ `unhandled error in route` なし。対照：浅い入れ子 → 201）。**赤**：500 と `unhandled error in route` | `2f1e599` |
| **I1**（潜在）：`api/validation.ts` の `toValidationDetails` が `(details[key] ??= []).push(...)`。1 段のパスのキーが `constructor` などだと継承した関数を拾って `TypeError` | 直す | `domain/text.ts` と同じく `Map` に積んで `Object.fromEntries` で**自分のプロパティ**として返す | `api/api-foundation.test.ts` の I1（最上位 `z.record` に `{"constructor":1}` ほか 4 名前 → 例外なし・`ok: false`・`details` がその名前 1 つ。`superRefine` が 1 段のパスで返す誤り 5 名前（`__proto__` を含む）→ その名前 1 つ。`__proto__` でも原型が差し替わらない。同じ名前の 2 つの誤りが 1 つのキーにまとまる）。**赤**：`TypeError: details[key].push is not a function` | `9996f2b` |
| **I2**（潜在）：`application/social/social-use-cases.ts` の `validate()` の結果の積み方が同じ形。Plugin が利用者の `providerOptions` のキーを `field` に返すと `constructor` などで `TypeError` → 500 | 直す | 同じ（`Map` → `Object.fromEntries`）。`field` の形の検査（`VALIDATE_FIELD_PATTERN`）と丸め先は変えない | `api/social-post-create.integration.test.ts` の I2（`field` が `constructor`・`toString`・`valueOf`・`hasOwnProperty` → 422・`details` がその名前 1 つ ／ 同じ名前 2 つ → 1 つのキーに 2 文言 ／ ログなし。`__proto__` は `field` の形（英字で始まる）に合わないので従来どおり `providerOptions` に丸めて 422）。**赤**：500 | `2f1e599` |
| **I3**：使えない文字のたどり方の `seen` が最上位の項目ごとで、共有された部分木を項目の数だけたどり直す | 記録だけ | 設計 §13 の 9。共有した値を渡せるのは内部の呼び出し（UseCase・Data API・Store を直接呼ぶ信頼されたコード）だけ。HTTP の本文は `JSON.parse` の木で共有が無い | — | 文書のコミット |
| **I4**：たどるときに getter・`Proxy` が投げる例外を捕まえない | 記録だけ | 設計 §13 の 10。そういう値を渡せるのは信頼された Plugin のコードだけで、例外は呼んだ Plugin へ返る | — | 文書のコミット |

**Zod 4 の `z.record` について**：I1 のテストは最初 `{"__proto__":1}` も `z.record` で確かめるつもりだったが、Zod 4 の `z.record` は `__proto__` のキーを読み飛ばして成功を返す（誤りが出ない）。
`__proto__` は `superRefine` が 1 段のパスで返す誤りで確かめる形に、実装の前に直した（テストの前提の誤りで、弱めたのではない）。

**件数**：`pnpm test`（CI と同じ `DATABASE_URL`・`TORIFUNE_ENCRYPTION_KEY`）**253 ファイル / 11,018 件、すべて通過**（1 回目の処置の後の 10,935 件に、N1 56・I1 15・N2 5・I2 7 の 83 件を足した数と一致）。
`pnpm typecheck`・`pnpm lint`・`pnpm format:check`・`pnpm build` は通過。`plugins/`・`migrations/`・`packages/plugin-api` の差分なし。
変えたファイルに NUL・対になっていないサロゲート・U+FFFD の文字そのものが無いことをバイト列で確かめた（テストの本文は JSON のエスケープで書く）。

**E2E**：`api/route.ts` を変えたので、処置のコミットの後に全件を流した。1 回目の処置と同じ手順（`039` 実装プラン §8 の 35。`git archive HEAD`（`2f1e599`）を
`mcr.microsoft.com/playwright:v1.62.1-noble` のコンテナへ展開し、`pnpm install --frozen-lockfile` → `pnpm generate:plugins` → `pnpm migrate` → `pnpm test:e2e`。
DB は作業機の postgres-test を `host.docker.internal:21701` で使う。`CI` は立てず再試行なし。`--rm`。ほかの重い処理と重ねない）。
1 回目は 495 件通過・1 件失敗：`jobs.spec.ts` の「ロック保持中に 5 本同時に投げると…」が、テスト自身の前提の確認（`定期実行と重なった。やり直す`。1 分おきの定期実行がロックを先に取った）で落ちた。046 の変更とは関係しない。
同じ手順でもう一度流し、**496 件すべて通過**（3.7 分）。既存の E2E は変えていない。
