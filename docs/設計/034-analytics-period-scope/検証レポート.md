# 034 アナリティクスの期間プリセット「昨日」と期間の適用範囲 — 検証レポート

対象：`要件.md`（裁定）・`設計.md`（§1〜§12）・`実装プラン.md`（T1〜T13）と、受け入れ条件 #1〜#78。
検証日：2026-09-09。ブランチ `feature/034-analytics-period-scope`（`main` から分岐）。

検証は `spec-verifier` / `boundary-guardian` / `security-reviewer` の 3 つを**互いの結果を見せずに独立して**
実行した。以下はその 3 つの報告をまとめたものである（3 エージェントは読み取り専用。何も書き換えていない）。

## 1. 判定

**3 件とも合格。重大な指摘は 0 件。**

| 検証 | 判定 | 重大 | 軽微・情報 |
| --- | --- | --- | --- |
| `spec-verifier`（設計との整合） | 合格 | 0 | 4 |
| `boundary-guardian`（責務境界・API 互換） | 合格（境界違反なし） | 0 | 3（判断が分かれる箇所） |
| `security-reviewer`（セキュリティ） | 合格 | 0 | 1（低） |

| 検査 | 結果 |
| --- | --- |
| `pnpm test` | **119 ファイル / 2960 件すべて通過**（失敗 0・スキップ 0） |
| `pnpm typecheck` | 通過（`plugin-api` / `cli` / `web`） |
| `pnpm lint` | 通過 |
| `pnpm format:check` | 通過 |
| `pnpm test:e2e` | **388 passed / 2 failed**（§5 のとおり、落ちた 2 件は 034 と無関係の既存不具合） |

`pnpm test` は `TORIFUNE_TEST_DATABASE_URL`（`postgres-test`）・`TORIFUNE_ENCRYPTION_KEY`・
`DATABASE_URL` を与えて実行した。与えないと `route-error-redaction` / `timezone.integration` /
`run-job.integration` が落ちるが、これは本設計と無関係な実行環境の前提である。

## 2. 受け入れ条件の対応（#1〜#78、取りこぼし 0）

| 受け入れ条件 | テスト | 層 |
| --- | --- | --- |
| A #1〜#11 | `domain/analytics/day.test.ts`（追記） | ユニット |
| B #12〜#14 / C #15〜#23 | `ui/analytics/labels.test.ts`（新規） | ユニット |
| D #24〜#28 / H #70 / I #76 | `ui/analytics/analytics-query.test.ts`（追記） | ユニット |
| E #29〜#30 | `ui/analytics/overview-tab.test.ts` / `visitors-tab.test.ts`（既存が無修正で回帰） | ユニット |
| E #31〜#37 | `e2e/analytics.spec.ts`（新規 `describe('期間の適用範囲')`）＋静的検査 | E2E ＋ 静的 |
| F #38 / #40〜#42 | `ui/analytics/analytics-view.test.ts`（`PeriodBar` / `shouldShowPeriodBar`） | ユニット |
| F #39 / #43 / #44 | `e2e/analytics.spec.ts` ＋ 静的検査 | E2E ＋ 静的 |
| F #45〜#53 | `ui/analytics/parts.test.ts`（新規）・`pages-tab.test.ts`（新規）・`referrers-tab.test.ts`（新規）・`overview-tab.test.ts` / `visitors-tab.test.ts` / `settings-tab.test.ts`（追記） | ユニット |
| G #54〜#63 | `e2e/analytics.spec.ts`（期間内＝昨日／期間外＝一昨日を直挿し） | E2E |
| G #64 | `application/analytics/analytics.integration.test.ts`（追記） | 結合 |
| H #65〜#69 / #71 | `e2e/analytics.spec.ts`（既存 030 テストへ `?period=yesterday` の往復を追加＋新規） | E2E |
| I #72〜#78 | 既存 E2E・`static-checks.test.ts`・`dashboard.spec.ts`（無修正で通ること） | E2E ＋ 静的 |

**すべてテストを先に書き、実装で通した。テストは 1 件も書き換えていない。**
テスト先行の時点で 106 件が意図どおり失敗し（034 の 10 ファイルのみ）、実装後に全件通過した。

### 2.1 層を移した 2 件（実装プラン §8 の明示的な選択）

* **#41**（`empty-period` では `PeriodBar` を出さない）… 実行日が月の 1 日でないと E2E で作れないため、
  純粋な述語 `shouldShowPeriodBar('empty-period')` のユニットで決定的に担保した
  （030 の `shouldShowStaleRangeNotice` と同じ扱い）
* **#70**（今日が月の 1 日でも「昨日」は空にならない）… 同じ理由で
  `resolvePeriod(params, '2026-09-01')` のユニットで担保した

### 2.2 空振り検知の対

`#34`（2 日なら両方出る）・`#63`（`?period=7d` に広げると期間外の値が現れる）・`#46`（`caption` 無しの DOM）・
`#22`（`analyticsHeaderText` が `rangeText` を使い続けている）・`#64`（Repository の期間条件が変わっていない）は、
いずれも「実装を消しても通ってしまう」ことが起きないための対として置かれている。

## 3. 指摘事項

### 3.1 重大

**3 件とも 0 件。**

### 3.2 軽微・情報（`spec-verifier`）

1. `docs/設計/030-analytics-today/設計.md` の追記見出し 1 箇所の前に空行が無く、他 8 箇所と体裁が揃っていない
   → **対処済み**（設計担当へ差し戻して修正）
2. 実装プラン §9「完了の定義」の文言と実際が形式上ずれている 2 点
   （`application/**` にテストの差分はある／`rangeText` は JSDoc のみ加筆）。本番コードの差分は無い
   → **文言の問題のみ。実装は設計どおり**
3. E2E #69 のアサーションが条件付き（案内が出ない実行では走らない）。定期ロールアップとの競合を避ける
   意図的な形（030 #63 と同じ扱い）で、文言そのものは `labels.test.ts` #23 と静的検査が決定的に固定している
4. `days === 1` への一般化は **`?period=month` を月の 2 日に選んだときにも及ぶ**（今月 1 日〜昨日＝1 日）。
   設計の意図どおりの挙動だが、設計書は `custom` の 1 日だけを副作用として書いていた
   → **対処済み**（設計 §7.2 と 030 §7.3 への追記に 1 行ずつ追加）

### 3.3 判断が分かれる箇所（`boundary-guardian`）

1. `shouldShowPeriodBar` が**拒否リスト**（`kind !== 'settings' && kind !== 'empty-period'`）である。
   将来 `TabData` に新しい `kind` が増えると、既定で `PeriodBar` が出る側に倒れる。
   設計 §7.3.2 の表を素直に写した形であり、境界違反ではない。**今回は変えない**
2. `period=custom` の 1 日でも「日次の推移」「1日あたり訪問者」が消える（利用者から見える挙動変更）。
   ユーザー裁定済みで、030 §7.3 への改訂として記録してある。**境界の問題ではない**
3. `staleRangeNoticeText` が `rangeText` → `dateRangeText` に変わった。Plugin から見える契約ではない

### 3.4 低（`security-reviewer`）

1. 実装プランにローカル DB の接続文字列（`postgresql://torifune:torifune@localhost:21701/...`）が入る。
   ただし `.env.example` / `README.md` / `.github/workflows/ci.yml` に同じ値が既にあり、
   `localhost` のテスト用固定値である。**034 が新たに開けた穴ではない**（見直すなら別件）

## 4. 境界とセキュリティの確認結果（要点）

* `packages/plugin-api/` に**差分が 1 バイトも無い**。`PLUGIN_API_VERSION` は `1` のまま
* `PeriodPreset` / `presetRange` / `PERIOD_PRESETS` の参照は `domain/analytics/day.ts` と
  `ui/analytics/` の 3 ファイルだけ。**Plugin からも API からも見えない**。
  Plugin が触る `AnalyticsQuery`（Plugin Data API）と `GET /api/v1/analytics` は `from` / `to` のみ
* 「昨日」は既存プリセットと同じ確定値経路（`analytics`）を通るので、
  **昨日の Plugin 由来の点（`source <> 'core'`）は他プリセットとまったく同じに出る**
* `SectionHeader` の参照元は `ui/analytics/` 配下のみ。アナリティクス画面に Extension Point は 1 つも無く、
  props の変更が Plugin から観測されることはない
* `migrations/` `api/` `app/api/` `application/`（本番コード）`infrastructure/` `packages/` `.env*`
  `package.json` `compose*` に差分なし。**新しい環境変数・起動時要件を増やしていない**（単体成立性を保つ）
* 期間は外部入力だが、検証は `isPeriodPreset` の allowlist のまま。プリセット経路は `from` / `to` を読まないので
  `?period=yesterday&from=…&to=…` で 400 日の上限を回避することはできない
* `PeriodBar` / `caption` に出るのは検証済みの `YYYY-MM-DD` と列挙値だけで、サイト名・公開キー・権限は入らない。
  React が自動エスケープし、`dangerouslySetInnerHTML` の実使用はリポジトリ全体で 0 件
* `analytics.read` が無ければ `AnalyticsView` に到達せず `PeriodBar` も描かれない（E2E #65 が固定）。
  `siteId` 差し替えの防御（404 と UseCase 側の認可）は 1 文字も変えていない

## 5. E2E の 2 件の failed について（034 と無関係）

`pnpm test:e2e` の全件実行は **388 passed / 2 failed**。落ちたのは次の 2 件である。

| spec | 内容 |
| --- | --- |
| `e2e/api-foundation.spec.ts:85` | OpenAPI のパス一覧に `/settings/access-log-ips` が無い |
| `e2e/settings-tabs.spec.ts:35` | 設定「一般」タブの「保存する」ボタンが 2 個で strict mode violation |

**`git stash -u` で 034 の変更を全部退避した状態（＝ `main` 相当）で同じ 2 spec を流し、
同じ 2 件が落ちることを確認した**（23 passed / 2 failed）。原因は既に `main` へマージ済みの
033（`0497717`）が、`/settings/access-log-ips` の追加と「一般」タブへの 2 つ目のフォーム追加に対して
E2E 側を更新していないことである。

**034 では手を付けていない。** 別件（`fix/033-...`）として扱う。

## 6. 検証の範囲

* 作業ツリーの全差分（変更 19 ファイル ＋ 新規 5、2029 行追加）
* `pnpm test` / `typecheck` / `lint` / `format:check` の実行（3 エージェントがそれぞれ独立に実行）
* E2E は**実装担当が 1 回だけ全件実行**した（同時に 1 つしか流せないため、検証 3 件では実行していない）。
  検証側は E2E のコードを読んで受け入れ条件との対応・値の計算（合算値を含む）を確かめた
* `packages/plugin-api/` の公開面、`ui/plugin/` の受け渡し、`api/schemas/analytics.ts`
* リポジトリ全体の `dangerouslySetInnerHTML` / `innerHTML` / `eval` / Secret の grep
