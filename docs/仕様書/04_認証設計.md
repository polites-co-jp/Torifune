# 04_認証設計.md

## 1. 目的

本ドキュメントでは、Torifune OSSにおける認証・認可の基本方針と、認証方式を拡張するための仕組みを定義する。

Torifune OSSは単体で利用可能な認証機能を標準提供するとともに、Pluginによって外部認証基盤へ接続できる構造とする。

---

## 2. 基本方針

Torifune OSSでは、以下を基本方針とする。

1. Torifune単体でログイン・ログアウトが完結すること
2. 認証処理をTorifuneの各機能から分離すること
3. 認証方式をAuthentication Providerとして抽象化すること
4. Pluginによって認証方式を差し替えられること
5. 認証済みユーザーの識別情報を、Torifune内部で統一的に扱えること
6. 認証と認可を分離すること
7. パスワードや認証Token等の機密情報を安全に管理すること

---

## 3. 認証と認可

認証（Authentication）と認可（Authorization）は別の責務として扱う。

### 認証

「誰であるか」を確認する処理。

```text
ユーザー
  ↓
ログイン
  ↓
Authentication Provider
  ↓
認証成功
  ↓
User Identity
```

### 認可

「そのユーザーが何をしてよいか」を判定する処理。

```text
User Identity
     ↓
Authorization
     ↓
Permission / Role
     ↓
操作を許可・拒否
```

認証方式を変更しても、Torifune内部の認可処理は可能な限り変更せずに利用できる構造とする。

---

## 4. 認証アーキテクチャ

認証処理はAuthentication Provider Interfaceを介して利用する。

```text
┌──────────────────────────────┐
│       Torifune Core      │
│                              │
│ Application / Authorization  │
└──────────────┬───────────────┘
               │
               ▼
     Authentication Interface
               │
       ┌───────┴────────┐
       ▼                ▼
Default Provider   Plugin Provider
       │                │
       ▼                ▼
   Local Auth       OIDC / OAuth
```

Torifune Coreは、具体的な認証方式を直接認識しない。

---

## 5. 標準認証

Torifune OSSは、外部認証サービスを必要としない標準認証を提供する。

標準認証では、Torifuneがユーザー情報および認証情報を管理する。

基本的な認証フローは以下とする。

```text
Browser
   │
   │ Login
   ▼
Torifune
   │
   │ Credentials
   ▼
Authentication Provider
   │
   │ Verify
   ▼
User Database
   │
   ▼
Authenticated User
   │
   ▼
Session
```

---

## 6. パスワード管理

ユーザーのパスワードは平文で保存しない。

データベースには、安全なPassword Hashのみを保存する。

```text
Password
   │
   ▼
Password Hash Function
   │
   ▼
Password Hash
   │
   ▼
Database
```

パスワードハッシュアルゴリズムは、実装時点で十分な安全性を持つ方式を採用する。

候補としてArgon2id等を検討する。

パスワード検証時には、入力されたパスワードを同じ方式で検証し、保存されたHashと比較する。

---

## 7. セッション管理

認証成功後は、ユーザーの認証状態を維持するためのセッションを作成する。

基本構成は以下とする。

```text
Browser
   │
   │ Secure Cookie
   ▼
Torifune
   │
   ▼
Session
   │
   ▼
User Identity
```

セッションCookieには、ユーザー情報そのものやデータベース接続情報等を格納しない。

Cookieには、セッションを特定するためのランダムで推測困難な識別子のみを保持する方式を基本とする。

---

## 8. セッションCookie

認証セッションに使用するCookieには、以下の属性を設定する。

* `Secure`
* `HttpOnly`
* `SameSite`

HTTPS環境では必ず`Secure`を利用する。

`HttpOnly`を設定することで、JavaScriptからセッションCookieを直接取得できないようにする。

`SameSite`については、Torifuneの利用形態に応じて適切な値を設定する。

---

## 9. セッション固定攻撃対策

ログイン前後で同一のセッション識別子を使用し続けることは避ける。

ログイン成功時にはセッションIDを再生成する。

```text
Before Login
     │
     ▼
Session ID A
     │
   Login
     │
     ▼
Session ID B
     │
     ▼
Authenticated Session
```

これにより、Session Fixation攻撃への対策とする。

---

## 10. ログアウト

ログアウト時には、サーバー側の認証セッションを無効化する。

```text
Authenticated Session
        │
        ▼
      Logout
        │
        ▼
Session Revoked
```

ブラウザ側のセッションCookieも削除する。

---

## 11. セッション有効期限

セッションには有効期限を設定する。

最低限、以下を区別できる設計とする。

* セッションの有効期限
* アイドルタイムアウト
* Remember Me等の長期ログイン

長期セッションを利用する場合は、通常のセッションとは異なるToken管理方式を検討する。

---

## 12. CSRF対策

Cookieベースの認証を利用する場合、CSRF対策を実装する。

基本的には、状態を変更するHTTPリクエストに対してCSRF Tokenを利用する。

```text
Browser
   │
   ├── Session Cookie
   │
   └── CSRF Token
          │
          ▼
      Torifune
```

GET等の安全なHTTPメソッドと、POST/PUT/PATCH/DELETE等の状態変更処理を適切に区別する。

---

## 13. XSS対策

認証セッションを保護するため、XSS対策を実施する。

特に以下を基本とする。

* セッションCookieへの`HttpOnly`設定
* ユーザー入力値の適切なエスケープ
* HTMLへの直接出力を避ける
* Content Security Policy（CSP）の導入を検討する
* JavaScriptへ機密情報を渡さない

Pluginによって追加された画面についても、同じセキュリティ原則を適用する。

---

## 14. 認可

認証されたユーザーに対して、Torifune内部の権限モデルに基づいて操作可否を判定する。

基本構成はRBAC（Role-Based Access Control）を採用する。

```text
User
  │
  ▼
Role
  │
  ▼
Permission
```

例えば、

```text
Administrator
├── user.manage
├── site.manage
├── social.manage
├── plugin.manage
└── system.manage

Editor
├── site.read
├── site.write
├── social.read
└── social.write
```

具体的なRoleおよびPermissionは、機能要件に応じて定義する。

---

## 15. Authentication Provider

Authentication Providerは、Torifuneの認証方式を抽象化するためのPlugin Extension Pointである。

基本的なInterfaceとして、以下のような責務を想定する。

```text
AuthenticationProvider
├── authenticate()
├── logout()
├── getIdentity()
└── refresh()
```

実際のInterface名・メソッド名は実装言語およびフレームワーク確定後に決定する。

Authentication Providerは、認証処理を実行した結果をTorifune共通のUser Identityへ変換する。

---

## 16. User Identity

認証方式にかかわらず、Torifune内部では統一されたUser Identityを利用する。

```text
Local Authentication
       │
       ▼
   User Identity
       ▲
       │
OIDC Authentication
```

User Identityには、例えば以下の情報を持たせる。

* Torifune User ID
* 外部認証Provider ID
* 外部ユーザーID
* 表示名
* メールアドレス
* 認証済み状態

ただし、外部Provider固有の情報をTorifune Coreが直接扱うことは避ける。

---

## 17. OpenID Connect

外部認証との連携では、OpenID Connect（OIDC）を第一候補とする。

SaaS版では、ユーザー管理アプリケーションをOIDC Providerとして動作させ、Torifune側にOIDC Authentication Provider Pluginを実装する構成を想定する。

```text
┌──────────────────┐
│ User Browser     │
└────────┬─────────┘
         │
         │ Authentication
         ▼
┌──────────────────┐
│ User Management  │
│ OIDC Provider    │
└────────┬─────────┘
         │
         │ ID Token / Authorization Code
         ▼
┌──────────────────┐
│ Torifune     │
│ OIDC Plugin      │
└──────────────────┘
```

Torifuneは、ユーザー管理アプリケーションの認証情報そのものを保持せず、OIDCによって認証結果を受け取る。

### リダイレクト往復の受け口（Core が提供する）

OIDCはブラウザのリダイレクト往復で成立する。この往復のうち、**Core が受け口と検証を持つ**。

```text
Browser ──► Core: 認可開始      ──► Plugin: 認可URLを組む ──► 外部IdP
Browser ◄── 302 認可URL
Browser ──► 外部IdP ──► 302 ──► Core: コールバック ──► Plugin: Token検証 ──► Core: セッション発行
```

* **認可開始**：`GET /api/v1/auth/authorize`。Core が State と Nonce と Redirect URI を発行し、
  Pluginの `startAuthorization()` へ渡す
* **コールバック**：`GET /api/v1/auth/callback`。Core が State を照合してから
  Pluginの `completeAuthorization()` を呼ぶ

Plugin は `login.methods` 拡張点（`06_画面設計.md` §5）へ「Googleでログイン」等のボタンを差し込み、
認可開始の口へリンクする。パスは公開Plugin APIの `AUTHORIZATION_START_PATH` として提供する。

責務の境界は §27 の表を正とする。詳細は `docs/設計/025-redirect-authentication/設計.md`。

---

## 18. OAuth 2.0との関係

OAuth 2.0は主に「認可」を扱うプロトコルであり、ユーザー認証そのものを目的としたプロトコルではない。

そのため、Torifuneのユーザー認証に外部Identity Providerを利用する場合は、OAuth 2.0単体ではなく、OpenID Connectを利用することを基本とする。

```text
OAuth 2.0
  └── Authorization

OpenID Connect
  └── Authentication
```

外部サービスのAPIアクセス権を取得する目的では、OAuth 2.0を利用するPluginを別途実装できる。

---

## 19. SaaS版における認証

Torifune SaaSでは、SaaS側のユーザー管理システムをIdentity Providerとして利用する。

```text
Torifune SaaS
│
├── User Management
│
├── OIDC Provider
│
└── Torifune
      │
      └── OIDC Authentication Plugin
```

Torifune OSS本体には、SaaS固有のユーザー管理ロジックを実装しない。

---

## 20. 認証とDatabase選択の分離

Torifune OSSでは、認証とDatabase接続を別の責務として扱う。

```text
Authentication
      │
      ▼
User Identity
      │
      ├──────────────────┐
      │                  │
      ▼                  ▼
Authorization      Database Provider
```

SaaS版では、認証されたUser IdentityやTenant情報を利用してDatabase Providerが接続先を決定することが可能となる。

ただし、Torifune Coreの認証処理が直接Databaseを選択することはない。

この分離により、OSS版では単一Databaseを利用し、SaaS版ではPluginによってTenant単位のDatabase構成へ拡張できる。

---

## 21. 認証情報の受け渡し

Torifune SaaSとTorifune OSSの間で認証情報を連携する場合、認証情報そのものを独自方式で受け渡すのではなく、標準プロトコルを利用する。

第一候補はOpenID Connectとする。

```text
Browser
   │
   │ 1. Login Request
   ▼
User Management / OIDC Provider
   │
   │ 2. Authentication
   │
   │ 3. Authorization Code
   ▼
Browser
   │
   │ 4. Callback
   ▼
Torifune OIDC Plugin
   │
   │ 5. Token Exchange
   ▼
OIDC Provider
   │
   │ 6. ID Token / UserInfo
   ▼
Torifune
```

Torifuneがユーザー管理アプリケーションのセッションCookieを直接受け取ったり、セッション情報を共有したりする方式は採用しない。

### この往復で Core と Plugin が受け持つもの

上図の1〜6を、実装上どちらが担うかは次のとおり。

| 図の段 | 担当 |
| --- | --- |
| 1. Login Request（認可開始の入口） | **Core**（`GET /api/v1/auth/authorize`） |
| 認可要求の組み立て（`client_id` / `scope` / PKCE 等） | Plugin（`startAuthorization()`） |
| `state` / `nonce` / `redirect_uri` の発行 | **Core**。Plugin は渡された値をそのまま使う |
| 2-3. 外部での認証と Authorization Code の発行 | 外部Provider |
| 4. Callback の受け口 | **Core**（`GET /api/v1/auth/callback`） |
| `state` の照合・使い捨て・期限切れ判定 | **Core** |
| `redirect_uri` が発行時と同一かの照合 | **Core** |
| 5. Token Exchange | Plugin（`completeAuthorization()`） |
| 6. ID Token / UserInfo の検証 | Plugin（§23） |
| 外部の識別子 → Torifuneのユーザーの解決 | Plugin |
| そのユーザーが実在することの確認 | **Core** |
| セッションの確立 | **Core**（§22） |

**State・Nonce・Redirect URI の発行と照合をPluginごとに実装させない。**
どれか1つのPluginの実装ミスが、その環境の認証全体の穴になるため。

---

## 22. Token管理

外部認証で利用するTokenは、用途を明確に区別する。

### ID Token

ユーザーが誰であるかを確認するために利用する。

### Access Token

外部APIへのアクセス権を利用するために使用する。

### Refresh Token

Access Tokenを更新するために使用する。

Torifuneのログイン処理では、ID Tokenをユーザー認証結果として利用し、Torifune自身のセッションを確立する方式を基本とする。

### Tokenを持つのはPlugin、セッションを持つのはCore

ID Token / Access Token / Refresh Token は、**いずれもTorifune Coreが保持しない。**
これらは方式ごとに形も寿命も異なり、Coreが解釈するとCoreが特定の認証方式を知ることになる。
保管が必要な場合はPluginが自分のKey-Value Store（Secret種別）へ置く。

Coreが受け取るのは、Pluginが検証を終えたあとの **User Identity**（§16）だけである。

そのため公開Plugin APIには、**Pluginがセッションを発行する口を設けない。**
セッションの発行・ログイン時の再生成（§9）・有効期限（§11）・アイドルタイムアウト・失効は、
すべてCoreに残る。Pluginごとに散らすと、Session Fixation対策と失効の責任が
Pluginの数だけ分かれてしまう。

---

## 23. Token検証

OIDC等の外部認証を利用する場合、受け取ったTokenを無条件に信頼しない。

少なくとも以下を検証する。

* 発行者（Issuer）
* Audience
* 有効期限
* 署名
* nonce等、フローに応じた検証項目
* 必要なClaim

OIDC DiscoveryおよびJWKSを利用して、Providerの公開鍵情報を取得・管理する方式を基本とする。

### 検証を行う場所

**Token検証はPluginが行う。** Torifune CoreはJWTを解釈しない。
Discovery・JWKSの取得と鍵のキャッシュも同様にPluginの責務である。

ただしnonceだけは分担になる。

* **Coreが持つ**：nonceの発行、`state`への束縛、使い捨て、有効期限
* **Pluginが持つ**：ID Tokenの `nonce` Claimと、Coreから渡されたnonceの照合

Coreは「そのnonceが新鮮で、この往復専用で、一度きりであること」を保証し、
Pluginは「受け取ったTokenがそのnonceを含むこと」を確かめる。
Claimの照合をCoreへ寄せるにはCoreがJWTを解釈する必要があり、それは§22の方針に反する。

---

## 24. パスワードリセット

標準認証では、パスワードを忘れたユーザーが安全にパスワードを再設定できる機能を提供する。

基本フロー：

```text
Login Page
    │
    ▼
Forgot Password
    │
    ▼
Reset Token発行
    │
    ▼
メール等による通知
    │
    ▼
Reset Token検証
    │
    ▼
新しいパスワード設定
```

Password Reset Tokenは、

* 十分なランダム性を持つ
* 有効期限を持つ
* 一度使用したら無効になる
* 平文で永続保存しない

ことを基本とする。

---

## 25. ログイン試行制限

総当たり攻撃やCredential Stuffingへの対策として、ログイン試行回数を制限する。

例えば以下を検討する。

* IPアドレス単位のRate Limit
* アカウント単位のRate Limit
* 一定回数失敗後の遅延
* CAPTCHA等の追加認証
* 異常なログイン試行の記録

具体的な閾値は運用環境および実装時に決定する。

---

## 26. セキュリティログ

認証・認可に関する重要な操作を監査ログとして記録する。

対象例：

* ログイン成功
* ログイン失敗
* ログアウト
* パスワード変更
* パスワードリセット
* ユーザー作成
* ユーザー無効化
* Role変更
* Permission変更
* 外部認証連携設定変更

ログにはパスワード、Access Token、Refresh Token等の機密情報を記録しない。

---

## 27. Plugin Security

Authentication Provider Pluginは、Torifuneの認証処理を担う高権限Pluginである。

そのため、通常のPluginよりも厳格に扱う。

Authentication Provider Pluginでは特に以下を考慮する。

* 認証結果の改ざん防止
* Token検証
* Redirect URI検証
* CSRF対策
* State検証
* OIDCの場合のNonce検証
* Secretの安全な管理
* HTTPSの利用

### これらをどちらが実装するか

**上の項目のうち、State検証・Redirect URI検証・CSRF対策はCoreが実装する。**
Pluginごとに書かせない。**どれか1つのPluginの実装ミスが、その環境の認証全体の穴になる**ため。
「Pluginを信頼されたコードとして扱う」ことと、「間違えようのない形にする」ことは別である。

| 項目 | Torifune Core | Authentication Provider Plugin |
| --- | :---: | :---: |
| Stateの発行・保管・照合・有効期限・使い捨て | ○ | |
| Nonceの発行・Stateへの束縛・使い捨て | ○ | |
| NonceとID Tokenの `nonce` Claimの照合 | | ○ |
| Redirect URIの決定と、コールバック時の照合 | ○ | |
| コールバックのCSRF対策（State検証が代替する） | ○ | |
| 認可要求の組み立て（`client_id` / `scope` / PKCE 等） | | ○ |
| Token Exchange | | ○ |
| Token検証（iss / aud / exp / 署名 / Claim）・Discovery・JWKS | | ○ |
| Secret（`client_secret` 等）の保管 | | ○（Key-Value StoreのSecret種別） |
| 外部の識別子 → TorifuneのユーザーIDの解決 | | ○ |
| 認証結果の改ざん防止（実在するユーザーかの確認） | ○ | |
| ログイン後の遷移先の検証（Open Redirect対策） | ○ | |
| セッションの発行・再生成・有効期限・失効 | ○ | |
| 監査ログ（`login.succeeded` / `login.failed`） | ○ | |
| HTTPSの利用 | 運用（Reverse Proxy） | |

**コールバックは外部からのリダイレクトであるため、§12 のCSRF検証はそのままでは効かない。**
`Origin` は外部Providerのものになり、CSRFトークンは外部Providerが張るリダイレクトに載せられない。
そのままCSRF検証を当てると、正当なコールバックが必ず落ちる。
**その役目をState検証が代替する。** Stateは Core が乱数で発行し、ハッシュで保管し、
一度しか使えず、有効期限を持ち、Providerと Redirect URI に束縛されている。

---

## 28. 権限昇格対策

認証済みユーザーのIdentity情報を、ユーザーから自由に変更できる情報として扱わない。

特に以下はサーバー側で管理する。

* User ID
* Role
* Permission
* Tenant ID
* Authentication Provider
* 外部ユーザーID

クライアントから送信されたUser IDやRoleをそのまま信頼して認可判定を行ってはならない。

---

## 29. SaaS環境でのTenant分離

Torifune OSS自体は、マルチテナント構成を必須としない。

SaaS版でTenant単位のDatabaseを利用する場合は、Authentication Providerによって確定したIdentityと、SaaS側で管理するTenant情報を利用してDatabase Providerが接続先を決定する。

```text
User
 │
 ▼
OIDC Authentication
 │
 ▼
User Identity
 │
 ▼
Tenant Resolution
 │
 ▼
Database Provider
 │
 ▼
Tenant Database
```

重要なのは、**クライアントから送信されたTenant IDだけを根拠にDatabaseを選択しないこと**である。

TenantとUserの関係は、信頼された認証情報およびSaaS側の管理情報からサーバー側で確定する。

---

## 30. 設計上の責務境界

| 項目                | Torifune Core | Authentication Provider |            Plugin | SaaS |
| ----------------- | ----------------: | ----------------------: | ----------------: | ---: |
| 標準ログイン            |                 ○ |                       ○ |                   |      |
| セッション管理           |                 ○ |                       ○ |                   |      |
| 認証Interface       |                 ○ |                         |                   |      |
| 標準認証Provider      |                 ○ |                       ○ |                   |      |
| OIDC Provider     |                   |                         |                 ○ |      |
| 認証ユーザー識別          |                 ○ |                       ○ |                   |      |
| Role / Permission |                 ○ |                         |                   |      |
| ユーザー登録            |                 ○ |                         |                   |    ○ |
| SaaSユーザー管理        |                   |                         |                   |    ○ |
| Tenant管理          |                   |                         |                   |    ○ |
| TenantとUserの関連付け  |                   |                         |                   |    ○ |
| Tenant Database選択 |                   |                         | Database Provider |    ○ |
| SaaS認証基盤          |                   |                         |                   |    ○ |

---

## 31. 設計上の原則

Torifune OSSの認証設計では、以下を原則とする。

1. Torifune単体で認証できること
2. AuthenticationとAuthorizationを分離すること
3. 認証方式をAuthentication Providerとして抽象化すること
4. 認証後のユーザー識別情報をUser Identityとして統一すること
5. パスワードを平文で保存しないこと
6. セッションCookieをSecure・HttpOnly等の適切な属性で保護すること
7. ログイン時にセッションIDを再生成すること
8. Cookieベースの認証ではCSRF対策を行うこと
9. 外部認証には原則としてOpenID Connectを利用すること
10. OAuth 2.0とOpenID Connectの用途を混同しないこと
11. 外部認証Tokenを無条件に信頼しないこと
12. 認証情報とDatabase接続方式を分離すること
13. クライアントから送信されたUser IDやTenant IDをそのまま信頼しないこと
14. SaaS固有の認証・Tenant管理をTorifune OSS本体に持ち込まないこと
15. Authentication Providerを高権限Pluginとして扱うこと
