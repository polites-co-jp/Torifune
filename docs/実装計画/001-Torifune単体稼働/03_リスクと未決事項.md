# リスクと未決事項

---

## R-01. Plugin の「再ビルド＆自己再起動」方式が本番構成と噛み合うか（**検証済み：成立**）

> **スパイク結果（S0 で実施）**
>
> `Dockerfile` と `docker/entrypoint.sh` を実装し、コンテナ内で以下を確認した。
>
> | 検証項目 | 結果 |
> | --- | --- |
> | 稼働中のコンテナ内で `next build` が完走するか | ○ 成功（雛形アプリで 3 秒） |
> | 再ビルドで増えたページが再起動後に配信されるか | ○ 新規ページが 200 で返った |
> | 終了コード 75 を受けて supervisor が再ビルド→再起動するか | ○ ループが期待どおり動作 |
> | ビルド失敗時に直前の成功ビルドへ戻るか | ○ 退避した `.next` から復帰し、旧ビルドで配信を継続 |
> | ビルド時のピークメモリ | 約 **1.1 GiB**（雛形アプリの時点） |
> | イメージサイズ | 約 **1.3 GB**（devDependencies とツールチェーンを含むため） |
>
> **結論：D-02 の方式は成立する。** 以下は残る制約として引き続き扱う。
>
> * アプリの規模が大きくなるとビルド時間とメモリが増える。**S13 完了時点で再計測する**
> * 本番のコンテナにメモリ上限を設ける場合、ビルド分を見込む必要がある
> * ビルド〜再起動の間はサービスが停止する。メンテナンス表示が要る

### 当初の懸念（記録として残す）

D-02 で採用した導入方式は、以下をコンテナ構成に要求する。

| 要求 | 衝突しうる規定 |
| --- | --- |
| イメージにビルドツールチェーンを同梱する（Next.js の standalone 出力が使えない） | イメージサイズと攻撃面が増える |
| `plugins/` を書き込み可能な永続ボリュームにする | `06_コンテナ設計.md` §71「Read-only Filesystem」 |
| ビルド時に 1〜2GB のメモリを確保できる | 本番のリソース設計 |
| ビルド中〜再起動中はサービスが停止する | `07_デプロイ設計.md` §75「Maintenance Mode」 |
| ビルド失敗時に直前の成功状態へ戻せる | `07_デプロイ設計.md` §51「Application Rollback」 |

**採用した緩和策**（`Dockerfile` / `docker/entrypoint.sh` に実装済み）

* 書き込み可能にするのは `plugins/` ボリュームとビルド出力先だけに限定し、
  それ以外は read-only を維持できる構成にした
* ビルド前に `.next` を退避し、失敗したら書き戻して直前の成功ビルドで起動を続ける（実装済み）
* ビルド中はメンテナンス表示を返す（S12 で実装する）

`docker/entrypoint.sh` は起動・ビルドのコマンドを環境変数で差し替えられるようにしてある。
スタブを渡すことで、実際に Next.js をビルドせずにループとロールバックの挙動を検証できる。

---

## R-02. 「マーケット」（Plugin Registry）が未定義

D-02 の導入フローの起点である「マーケットからダウンロード」の**出所が決まっていない**。
`03_プラグイン設計.md` §15 は Plugin Registry を将来構想として書いているだけで、
実体・URL・配布形式・署名方式のいずれも未定。

**この計画での扱い**：マイルストーンAでは以下だけを実装する。

* ローカル配置（`plugins/<plugin-id>/` に人が置く）
* URL または ZIP ファイル指定による取り込み（**署名検証なし**）

**未決**：公式 Registry を作るのか、GitHub Releases 等を使うのか、
第三者 Registry を許すのか。Registry と署名検証は `020-plugin-registry` として次計画へ送る。
それまでは管理画面に「導入元の信頼性は利用者の責任である」旨を明示する。

---

## R-03. `CLAUDE.md` の記述更新（**S0 で対応済み**）

| 節 | 変更内容 | 状態 |
| --- | --- | --- |
| 「技術スタック」 | 「未確定」としていた ORM・マイグレーション・バリデーションを D-03/D-04/D-13 で確定させた旨に更新 | ✅ |
| 「コマンド」 | 想定コマンドから実際の `package.json` のスクリプト名へ | ✅ |
| 「ディレクトリ構成」 | D-01 の pnpm workspace 構成へ | ✅ |
| 「依存の向き」 | ESLint で検査している境界を明記 | ✅ 追加 |
| 「認可を書く場所」 | D-06 を明記 | ✅ 追加 |

`CLAUDE.md` と `.claude/` は Prettier の対象外にしてある（整形で差分を出さないため）。

---

## R-04. 仕様書に残る旧称「MarketingHub OSS」

`CLAUDE.md`「呼称の統一」が指摘しているとおり、`docs/仕様書/` に旧称が残っている。
実装スプリントの中で場当たりに直すと差分が散らかるため、
**`docs/rename-marketinghub-to-torifune` ブランチで一括修正**し、
S1 に入る前に片付ける。上位仕様の記述内容そのものは変更しない（呼称のみ）。

---

## R-05. Permission 体系の具体値が仕様書に無い

`04_認証設計.md` §14 と `05_API設計.md` §14 は「機能実装時に定義する」としている。
S3（`003-authorization`）で確定させ、以降のスプリントはそれに従う。

**注意点**：Plugin が Manifest で要求する Permission も同じ名前空間を使う
（`03_プラグイン設計.md` §20.2）。Plugin 独自の Permission を後から追加できるよう、
Permission を固定の enum ではなくレジストリで管理する。

---

## R-06. SNS ドメインが「箱」に留まること

S9 で作る `social_accounts` / `social_posts` には、
**外部SNSとの実連携が入らない**（`01_アーキテクチャ設計.md` §12 により Plugin の責務）。
そのため、マイルストーンA の時点では「投稿を登録できるが実際には投稿されない」状態になる。

これは仕様どおりだが、デモや説明の際に誤解を招きやすい。
画面上に「連携Pluginが未導入のため実際の配信は行われません」を明示する。

---

## R-07. Server Component からの UseCase 直呼びと認可の一貫性

D-06 により、同じ UseCase が Server Component 経由と REST 経由の両方から呼ばれる。
**認可を UseCase に置く**ことでこれを担保する設計だが、
実装が進むと「Server Component では認証済み前提だから」と認可を省く誘惑が生じる。

**対応**：S3 の受け入れ条件に「Permission チェックを持たない UseCase が存在しない」
という一覧テストを含める。security-reviewer と boundary-guardian の常設チェック項目にする。

---

## R-08. TorifuneHub 側との同期タイミング

TorifuneHub の `006-torifune-auth-plugin` と `007-torifune-database-plugin` は、
Torifune の S10（`010-plugin-api`）が完了して `packages/plugin-api` が固まるまで着手できない。

**S10 完了時点で TorifuneHub 側へ通知する。**
それまで TorifuneHub 側は S0〜S5（雛形・コンテナ・System DB・OIDC・テナント管理）を進める。
TorifuneHub 側で Plugin API の不足を見つけた場合は、Hub 側で回避策を書かず、
Torifune 側の別セッションで拡張ポイントを追加する（両リポジトリの `CLAUDE.md` の規定）。

---

## S-4（2026-08-24 追加）Plugin の描画が例外を投げたときの扱い

`011-plugin-runtime` の検証で挙がった。

Plugin の Widget・拡張点は本体の Server Component と同じ文脈で描画する。
Plugin の描画が例外を投げると、**その画面が白くなる**（他の画面には波及しない）。

Error Boundary で Plugin の枠だけを落とす案があるが、Server Component では
Error Boundary が使えない。Plugin の枠を Client Component にする必要がある。

**`013-example-plugin` で実際の壊れ方を見てから決める。**
先に仕組みだけ作ると、要らない制約を Plugin 作者へ押し付けることになる。

---

## S-5（2026-08-24 追加）Plugin Package のアップロード上限と同時実行

`012-plugin-manager` の検証で挙がった。

`inspectPackage` は zip 全体をメモリへ載せる。上限（既定 32 MiB、
`TORIFUNE_PLUGIN_MAX_BYTES`）× 同時アップロード数だけ確保されうる。

`plugin.manage` を持つインスタンス管理者しか叩けないため実害は小さいが、
**上限を上げる運用では同時実行を絞る必要がある。**

決めるべきこと：

* 上限を上げてよい範囲の目安をドキュメントに書くか
* ストリーム展開へ変えるか（yauzl は対応している。検証を先に済ませる設計と
  両立させるには、一時ディレクトリへ出してから検証する形になる）

**急がない。** 既定の上限で困る Plugin が出てから決める。

---

## S-6（2026-08-24 追加）Authentication Provider の差し替え口が無い

`013-example-plugin` の検証で挙がった。

`PLUGIN_EXTENSION_KINDS` は `authentication` を含み、Manifest で宣言できるが、
**`PluginContext` に登録の口が無い。宣言しても使えない。**

`database` と同じ形で足せる。

```ts
context.authentication.registerProvider(provider);
```

**差し替えを実際に使うサンプルが無いまま口だけ作らない。**
使われない形を先に固めることになる。
外部認証（OIDC / LDAP 等）の Plugin を作るときに、その要求と合わせて決める。

それまでの間、`extensions: ['authentication']` を宣言した Plugin は
検証を通るが何もできない。

> **2026-08-31 追記**：「宣言しても使えない」ことを Plugin 開発ガイド §9 に書いた。
> 口を作るかどうかの判断は**保留のまま**。
> 差し替えの実物が無いうちに形を固めない、という当初の理由は変わっていない。

---

## S-7（2026-08-31 追加）仕様書にあり、どの計画にも載っていないもの

`docs/` と実装を突き合わせた際に見つかった。
`README.md` §2「次の計画へ送るもの」にも `01_スプリント計画.md`「次の計画に送るもの」にも
載っていないため、**このまま誰も拾わない状態だった。**

| # | 仕様 | 節 | 現状 |
| --- | --- | --- | --- |
| 1 | User API（`/api/v1/users` の CRUD） | `05_API設計.md` §15 | 未実装。`015-settings` に含める想定なら、そう書かれていない |
| 2 | Plugin 独自の Migration（`plugins/<id>/migrations/*.sql`） | `03_プラグイン設計.md` §19、`07_開発者向けガイド.md` §39 | 未実装。Key-Value Store（`plugin_store`）が代替として入っている |
| 3 | Data API の Users | `05_API設計.md` §22 | 未実装。Data API は Sites / Social のみ |
| 4 | 「メディア」画面 | `06_画面設計.md` §4 | 画面構成のツリーに1回出るきりで、以降どの節にも定義が無い |
| 5 | Remember Me（長期ログイン） | `04_認証設計.md` §11 | 未実装。有効期限とアイドルタイムアウトは実装済みだが、長期ログインだけ計画にも無い |
| 6 | Role変更 / Permission変更 / 外部認証連携設定変更 の監査ログ | `04_認証設計.md` §26 | `AUTH_AUDIT_EVENTS` に無い。操作自体が `015-settings` 待ち |

**決めるべきこと**

* #1 と #3 を `015-settings` の範囲に入れるか、別IDを起こすか
* #2 を実装するか、「Plugin のデータは `plugin_store` に置く」を仕様側の方針として確定させるか
  （後者なら `03_プラグイン設計.md` §19 と `07_開発者向けガイド.md` §39 の改訂が要る）
* #4 はコンテンツを Core の責務から外したとき（`改訂履歴.md` 2026-08-24）の
  外し漏れではないか。残すなら定義が要る
* #5 は Remember Me を仕様として残すか、`04_認証設計.md` §11 から落とすか
* #6 は `015-settings` の受け入れ条件に含める（操作を作るとき同時に監査ログも足す）

**関連**：`domain/auth-audit.ts` の `password.changed` / `user.created` / `user.disabled` は
宣言だけで発火箇所が無い。パスワード変更・ユーザー作成・ユーザー無効化の操作が
`015-settings` で入るときに繋ぐ。`system.manage` と同じ性質のもの。

> **2026-08-31 決定**：#1・#3・#5・#6 と、上の「関連」に挙げた監査イベント3つ、
> および `system.manage` の消費先は **`015-settings` の範囲に入れる**。
> ユーザー・権限管理の画面と一体で作らないと二度手間になるため。
> 内訳は `01_スプリント計画.md`「`015-settings` の内訳」に書いた。
>
> **#2（Plugin Migration）と #4（メディア画面）は未決のまま残す。**
> どちらも `docs/仕様書/` の改訂判断が要り、いま実害が出ていない。
> Plugin が実際に `plugin_store` で困るまで、メディアが実際に必要になるまで待つ。

---

## 2026-08-31 に片付けたもの

`docs/` の仕様と実装の突き合わせで見つかった不整合のうち、以下は解消した。

| 内容 | 対応 |
| --- | --- |
| ナビゲーションの「設定」が `/settings` を指すが画面が無く 404 になっていた | 項目を外した。`CORE_NAVIGATION` の行き先に画面が存在することをテストで縛った |
| `CORE_EXTENSION_POINTS` に描画先の無い `settings.tabs` / `login.methods` が載っていた | **一覧からは外さない**（削除は破壊的変更。`07_開発者向けガイド.md` §47、`010-plugin-api/設計.md`）。描画先が無いことを型定義のコメントと Plugin 開発ガイドに書き、どれが未描画かを `ui-shell.test.ts` の `PENDING` で固定した |
| 宣言と描画の食い違いを検出する仕組みが無かった | `ui-shell.test.ts` に「宣言した拡張点は、描画されているか PENDING に理由つきで載っている」を足した。画面を作って PENDING から外し忘れても落ちる |
| `social.edit.sidebar` に描画先が無かった | 投稿の編集画面（`/social/posts/[id]/edit`）に置いた |
| SNS投稿の画面が無く、`009-social` の受け入れ条件 #34 が未達だった | 一覧・作成・編集・削除を実装し、E2E 6件を足した。検証レポートの誤った `✅` も訂正した |
| `system.manage` を消費する箇所が無く、消してよいものか読み取れなかった | `domain/permission.ts` に「`015-settings` が消費する。未使用に見えても外さない」と書いた |
| `011-plugin-runtime` #32（Plugin ページの403表示）が S13 へ持ち越されたまま、013 の検証レポートにも記載が無かった | `e2e/plugin-example.spec.ts` に E2E を足し、011 の検証レポートを ✅ へ更新した |

S-6（Authentication Provider の差し替え口）については、**判断は保留のまま**、
「宣言しても使えない」ことを Plugin 開発ガイド §9 に明記した。
S-6 が指摘していた「ガイドに書いていない」はこれで解消したが、口を作るかどうかは未決のまま。
